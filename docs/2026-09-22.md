# 2026-09-22 — 工作日志

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 持久化排序基座精简定案文档 | ✅ | [031/017](infrastructure011/031.persistence-center/017.topic-sort-support-simplify.md) |
| 2 | 由其他 AI 按 017 编码 | ✅ | 验收通过（2026-09-22） |
| 3 | 主子表 API 对称化定案文档 | ✅ | [031/018](infrastructure011/031.persistence-center/018.topic-master-sub-api.md) |
| 4 | 由其他 AI 按 018 编码 | ✅ | 验收通过（2026-09-22） |
| 5 | 验收通过后刷新状态行 | ✅ | 017/018/011/015 头注已改「已落地」 |
| 6 | 再审文档硬伤 D1–D4 + 护栏 | ✅ | 015/013/011 已修；可选代码见 [019](infrastructure011/031.persistence-center/019.topic-persistence-followups.md) |
| 7 | **019 任务 A** 主子基座去后缀 | ✅ | `BaseMasterSubRepository`；调用方/常驻 docs/A1/A2 已跟；`mvn package` EXIT=0 |
| 8 | 单测补齐 + 全量 `mvn test` | ✅ | `BaseMasterSubRepositoryTest` + `SortSupportTest` + Webhook；EXIT=0 |
| 9 | **技术标准全仓审核** | ✅ | [2026-09-22-standards-audit.md](2026-09-22-standards-audit.md)；门禁 PASSED；017 两条例外；级联债另单 |
| 10 | **门禁 A/D/B 加固** | ✅ | 016 §11 映射补齐；`tools/package.json` + nvm 装 AST；未知 API 默认 POST |
| 11 | **平台 XLSX 导入导出（字典打样）** | ✅ | 025/032；批大小 500（非封顶）；POI 钉版；dev016 E2E 过 |
| 12 | **文档对齐审计（013/027·032·025·016·README·019）** | ✅ | CSV/`ImportRowError` 如实标注；IMPORT 审计进 016；main=`7cd69b5` 后继续提交 |

## 回归（审核收尾）

| 命令 | EXIT |
|------|------|
| `node tools/check-klsjnh-standards.mjs` | 0（先修 `JulyStorageProviderController` Javadoc） |
| `mvn -pl java17-infrastructure011 test` | 0 |
| `mvn -q package -DskipTests` | 0 |

已合入 `main`：`7cd69b5`（`ver 0.0.50`，WSL `dev016` 快进推送，无 GitHub PR）。后续文档对齐走 `./script011.sh`。

## 定案摘要

**017 排序**

- 删：`BaseRepository011` / `BaseTreeRepository011` / `BaseTreeSubRepository011`
- 留：`BasePo011` / `TreePo011`（不合并）
- 加：`SortSupport`；树默认 `sort_order, id`
- 补丁：M1 回收 Datasource/SyncRuleColumn；M2 含 coding-standards + iam-overview；C1–C5 已写入

**018 主子**

- 三分：主 / 子 / 主子（读写对称）
- `saveMaster` → insert\|update；加 `getMaster` / `getChildren`
- 子写：Replace=`saveChildren`；Incremental=子仓直调
- 树轴与主子轴正交；不强制用例迁基座

**019**

- 任务 A：`BaseMasterSubRepository`（已落地）
- 任务 B：暂缓
- 任务 C：限定域已落地（模型接入 / 同步规则）；字典日常 Incremental，导入 Replace 另案

- 不改 lowcode（低代码对齐在另一仓）；提交须 `./script011.sh`
