# 013 — 数据源中心 · 系统整体架构

> 状态：**能力一 / 二已实现；能力三 S1（单表）已实现，S2/S3 设计**。
> **模块边界**：`center-datasource011-starter` = management + Sql HTTP + Sync；core = kernel 端口；`java17-data-mybatis011-starter` = kernel 执行器。
> 三篇一套：[011 设计思路](011.topic-design.md) · [013 系统整体架构](013.topic-architecture.md) · [015 怎么使用](015.topic-usage.md)。
> 能力一底册（已实现）：[016 动态数据源](016.topic-datasource.md)。

## 011.分层总览

```
center-datasource011-starter
  web           JulyDatasourceController · JulySqlController · JulySyncController
  application   JulyDatasourceUseCase · SqlQueryUseCase · JulySyncRuleUseCase
  domain        management.* · sync.*（含 SyncEnginePort / Source / Sink）
  infrastructure management.{entity,mapper,repository,seed,runner}
                 sync.{entity,mapper,repository,SyncEngine011,SyncSource011,SyncSink011}

java17-core011（kernel 契约 + 公共）
  domain.datasource.kernel  ConnectionInfo · DynamicDataSourceRegistryPort · DataSourceProbePort
                            SqlRoutingPort · SqlDialectPort011 · TypeDialectPort011 · DatasourceSeedPort
                            ReloadResult / SeedResult011 / ColumnMeta
  common / config           DatabaseTypes011 · SqlGuard011 · KrtDatasourceConfig011（krt.ci011）

java17-data-mybatis011-starter（kernel 执行器）
  infrastructure.datasource.kernel  DataSourcePools011 · DynamicDataSource011 · RegistryImpl
                                    DataSourceProbe011 · SqlRoutingExecutor · SqlDialect011 + 4 方言
                                    TypeDialect011 + 4 类型方言
```

依赖铁律不变：`web → application → domain ← infrastructure`；center / data-mybatis 均依赖 core 端口。

## 013.能力一：多数据源管理（已实现）

> 详见 [016 动态数据源](016.topic-datasource.md)；本节只给架构定位。

- **表驱动**：`july_datasource` 为运行时唯一真源；`krt.ci011` 降级为**启动播种源**（表已有同 `dsCode` 则跳过、不复活墓碑；见 [016](016.topic-datasource.md) / requirement011/026）；
- **动态多实例**：`DynamicDataSource011`（`AbstractRoutingDataSource` + ThreadLocal 路由键 + 注册表），Druid 池**懒加载**（首次调用才建池）；
- **注册表**：`DynamicDataSourceRegistryPort.reloadAll(...)` → `ReloadResult`（配置未变则保留旧池）；
- **方言 SPI**：`SqlDialectPort011`（`dbType()` + `pageSql(sql, offset, pageSize)`）+ 注册表 `SqlDialect011`，内置 mysql / postgresql / oracle / sqlserver；新增库 = 加一个方言 bean；
- **探测**：`DataSourceProbePort` / `DataSourceProbe011`（临时池，不回灌注册表）；
- **播种**：`DatasourceSeedPort` / `DatasourceSeed011` / `DatasourceSeedRunner`（`existsIncludingDeleted` 判重，存在即跳）。

## 015.能力二：多数据源读写（分页）——设计

### 015.1 端口（参数化）

```
SqlRoutingPort（domain.datasource.kernel）
  selectListByPage(dsCode, sql, params, pageIndex, pageSize) → PageResult011<Map<String,Object>>   ★ 新增（参数化）
  selectList(dsCode, sql, params) · selectOne(dsCode, sql, params)                                  ★ 新增（参数化）
  execute(dsCode, sql, params)                                                                      ★ 写（不对外）
  （既有裸 SQL 变体保留，仅供内部：探针 / 管理面看数）
```

- **参数绑定**：`PreparedStatement` 真绑定（不是拼接）；这是"内核 → 对外"的前置；
- **分页**：`SqlDialectPort011.pageSql(...)` 生成方言分页 SQL + `COUNT(*)`；
- **clamp**：`pageSize ∈ [10, 500]`。

### 015.2 只读校验（归口框架）

`READ` 操作类硬校验（`probe` / `selectByPage` 共用）：必须以 `SELECT` 开头、禁 `;`、禁 DDL/DML 关键字。

### 015.3 操作类与分接口放权（未来）

| 操作类 `SqlOperation011` | 端点 | 现在 | 未来 |
|--------------------------|------|------|------|
| `READ` | `…/julySql/v1/selectByPage` | ✅ 落 | — |
| `WRITE` | `…/julySql/v1/execute` | ⏳ 不建/拒 | 独立权限码 |
| `DDL` | `…/julySql/v1/execute` | ⏳ 不建/拒 | 独立权限码 |

> 权限机制（注解 / IAM / Guard 端口）待定（[011 §019](011.topic-design.md)）。

## 016.能力三：同步体系——S1 已实现

> **类型与对照契约**（同步的依赖，先读）：[016 数据类型与对照契约](016.topic-type-contract.md)。

**已实现（S1 单表）**：`july_sync_rule` + `july_sync_rule_column` + `SyncEnginePort`/`SyncSourcePort`/`SyncSinkPort`（`SyncEngine011`/`SyncSource011`/`SyncSink011`）+ 规则 CRUD / `run` 端点；外库 SQL/表 → 本地表，按 `syncKey` upsert（E2E：read/written 一致、幂等、更新回写）。

```
sync.Endpoint        源/目标同构：{ dsCode, kind(sql/table/object), data }
sync.JulySyncRule    主表聚合：{ source, target, mode, syncKey, conflict, pageSize, options }
sync.JulySyncRuleColumn  子表聚合：{ sourceColumn, sourceType, targetColumn, targetType, transform }
sync.SyncSourcePort  readPage(Endpoint, pageSize, pageIndex) → rows
sync.SyncSinkPort    writePage(Endpoint, table, keyColumns, upsert, rows) → written
sync.SyncEnginePort  run(JulySyncRule) → SyncRunResult
```

- **方向无关**：`source` / `target` 只是端点，外部库与本地对象表同构；
- **对照 = 主子表**：主 `july_sync_rule` + 子 `july_sync_rule_column`（列 + 类型，见契约 [§017](016.topic-type-contract.md)）；
- **分步**：S1 单表 → S2 一主一子 → S3 一对多 / 多源（口子见契约 [§018](016.topic-type-contract.md)）；
- **复用方言**：分页读走 `SqlDialectPort011`；写入 / 列型走 `TypeDialectPort011`；
- **幂等**：按 `syncKey` upsert。

## 017.端点清单

**管理面（已实现）**：`/klsjnh/datasource/julyDatasource/v1`（insert / update / logicDelete / logicDeleteBatch / getById / selectListByPage / testConnection / reloadRegistry …）。

> `getByCode` **无 HTTP 端点**（程序内 `UseCase.getByCode`；与 `JulyConfig` 同口径）。管理面分页动词为 `selectListByPage`；Sql 只读分页为 `selectByPage`——有意命名差异。

**读写（只读已实现；写端点设计）**：`/klsjnh/datasource/julySql/v1`
| 端点 | 操作类 | 说明 |
|------|--------|------|
| `selectByPage` | READ | 参数化分页查询（只读） |
| `execute` | WRITE / DDL | ⏳ 未来，独立权限 |

**同步（S1 已实现）**：`/klsjnh/datasource/julySync/v1`（rule CRUD + run）；S2/S3 未来。

## 018.表结构

| 表 | 状态 |
|----|------|
| `july_datasource` | ✅ 已实现（DDL [docs/sql/july_center_datasource011.sql](../../sql/july_center_datasource011.sql)） |
| `july_sync_rule` | ✅ 已实现（同步规则**主表**：source/target/mode/syncKey/conflict；`options` JSON 扩展位预留；同文件） |
| `july_sync_rule_column` | ✅ 已实现（对照**子表**：源列/源类型/目标列/目标类型；同文件） |

## 019.关系与联动

- **与主库隔离（铁律）**：主数据源（MyBatis-Plus，承载 `july_*` 表）不感知动态数据源；动态库只能经 `SqlRoutingPort` 按 `dsCode` 访问；
- **与产品**：低代码 `importDataFromSql` / `executeSqlByPage` 是"同步 / 查询"的第一种特化，改为复用本中心；
- **与调度**：同步/重试接 [022 julyScheduler](../../requirement011/022.topic-july-scheduler.md)。
