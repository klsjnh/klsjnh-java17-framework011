# 013 — 项目结构与目录说明

> **BOM + 瘦 core + 中心/技术 starter** 共 15 个 Maven 模块承载 DDD 分层；**Java 包结构仍是六层洋葱且不变**（门禁按包名锚定）。中心纵切（access / platform / datasource / storage / message / ai）与调度整栈已迁出 core——本文描述现行归属。

## 1. 顶层结构

```
klsjnh-java17-framework011/
├── Agent.md                            # AI 协作门牌（进门先看什么、按什么顺序；协议见 docs/011.agreements.md）
├── pom.xml                             # 父 POM：纯聚合器（版本管理在 java17-bom011，消费方 import）
├── docs/                               # 知识库（体系与编号见 docs/011.agreements.md）
│   ├── 013.api-contract.md             # API 契约（信封 / 状态码 / 鉴权）
│   ├── 015.project-info.md             # 项目信息（定位 / 技术栈 / 构建运行）
│   ├── 016.coding-standards.md         # 编码规则（门禁映射）
│   ├── 017.tech-debt-redlines.md       # 技术债红线（参考实现审计）
│   ├── 019.backend-api-review.md       # 后端接口质量评审（契约层）
│   ├── deploy/                         # 容器化落地件（脚本 / Dockerfile / compose；机制见 infrastructure011/020）
│   ├── infrastructure011/              # 架构专题（011 选型·持久化规约 / 013 结构·访问中心 / 014 平台中心 / 015 配置·存储 / 016 消息 / 017 AI / 018 数据源 / 029 金标准 / 033 安全 / 020 容器化；旧中心底册已归档 archive011/）
│   ├── requirement011/ · requirement013/  # 业务设计 / 技术方案（主题同号贯穿）
│   └── sql/                            # DDL 唯一真源（base 模板 + july_*.sql；中心级 july_center_<center>011.sql）
├── script011.sh                        # 唯一操作入口（gate / push / build011 / dev011 / dev013 / stop / status / log）
├── tools/                              # 编码规范门禁（check；脚本只查不改，规则见 docs/016.coding-standards.md）
├── java17-bom011/                      # BOM（内部模块 + 三方版本）
├── java17-security-api011/             # 安全 API（纯 Java，零框架）
├── java17-core011/                     # ★ 瘦 core（common + 持久化基座 + 用户 + 端口契约 + 导出/导入/备份内核）
├── java17-security-autoconfigure011/   # 安全装配（JWT / 过滤器 / 审计切面；无 access 时放行 AuthorizationPort）
├── java17-security-starter011/         # 安全启动（聚合）
├── java17-data-mybatis011-starter/     # 数据启动（Druid + 动态数据源 kernel 执行器 + Mapper 自动装配）
├── center-datasource011-starter/       # 数据源中心（管理面 / Sql HTTP / Sync；kernel 端口在 core）
├── center-storage011-starter/          # 存储中心（管理面 + local011/minio011；object 端口契约在 core）
├── center-access011-starter/           # 访问中心（组织/角色/菜单/权限目录/AuthorizationAdapter；用户登录在 core）
├── center-platform011-starter/         # 平台中心（字典 julyDictionary + 系统配置 julyConfig；不引则无管理面）
├── java17-scheduler-quartz011-starter/ # 调度技术栈（管理面 + 执行审计 july_scheduler_audit/pk_mt + Quartz；非「调度中心」）
├── center-message011-starter/          # 消息中心（出入两套完整纵切面）
├── center-ai011-starter/               # AI 中心（完整纵切面，依赖 center-storage）
├── java17-observability011-starter/    # 可观测性
└── java17-app011/            # 参考应用（唯一 main + demo 样板；Seed 仅 src/test；当前 pom 未挂 storage/AI——NEG-TEST-OFF）
```

## 2. 模块与包结构

> 包名与类的归属（哪些包在哪个模块）见下；**依赖方向严格单向**：starter → core → security-api。

### ① java17-security-api011 — 纯 Java 安全 API（零框架依赖）

```
common.identity             # Operator011 操作人值对象（审计走 request attribute，无 ThreadLocal 全局上下文）
common.enums（部分）        # FrameworkStatus011（运行态三态；其余枚举仍在 core 的 common.enums）
domain.iam.auth             # AuthTokenPort / AuthorizationPort / PasswordPort / RuntimeStatusPort
```

### ② java17-core011 — `com.klsjnh.{common,domain,application,web,infrastructure}.*`（瘦 core）

**现行只留**：公共件、持久化基座、用户纵切、跨中心端口契约、导出/导入/备份内核。中心管理面已迁出。

```
common.enums / response / vo / exception / page / constant / util
                            # HttpCodeEnum011 / Status011 / AuditType011 / ExportFormat011 · Response011 信封
                            #   IdVo011 / BatchDelete*Vo011 · BusinessException · 分页对 · FrameConst011 + 开放字符串常量 · 纯工具
domain.shared               # EntityId / AuditInfo —— 跨域共享值对象
domain.iam.user             # ★ 仅用户聚合 / 仓储接口（登录·审计）；organization / menu / role / perm → center-access011-starter
domain.datasource.kernel    # 端口契约留 core（ConnectionInfo / RegistryPort / SqlRoutingPort / SqlDialectPort011 SPI / DatasourceSeedPort）；management / sync → center-datasource011-starter
domain.storagecenter.object # 端口契约（ObjectStoragePort / ObjectStorageProviderFactory SPI / StorageProviderCodes011）——管理面 → center-storage011-starter
domain.platform011          # export / importdata / backup（内核；业务 Provider 随各中心）
application.iam.user        # 用户用例 + @Transactional
application.platform011     # 导出 / 导入 / 备份用例（backup 软依赖存储）
infrastructure.config       # MybatisPlusConfig011（★ MP 3.5.9 分页插件）+ KrtDatasourceConfig011（krt.ci011）
                            #   + CoreAutoConfiguration011（imports 自注册：定向扫描 application/web/infrastructure，宿主零框架扫描）
infrastructure.persistence  # BasePo 家族 + AuditMetaObjectHandler + CommonMapper + BaseRepository 家族基座（8 形态）
infrastructure.iam.user     # JulyUserPo / Mapper / RepositoryImpl / 用户审计落地
web.iam                     # JulyUserController + VO（URL /klsjnh/iam/julyUser/**）
web.config                  # SpringDocConfig011（Swagger 分组 + 可选 app 组）
web.global                  # GlobalExceptionHandler · TraceIdResponseAdvice · WebPaths011 · AuditLog 注解 · AuthChainPresenceCheck011
web.util                    # Operator011Resolver / ClientIp011Resolver
```

> 已整体剥离（包仍可被 starter 扫入）：`domain/application/…` 下 organization·menu·role·perm → **access**；config·dictionary → **platform**；datasource management/Sql/Sync → **center-datasource**；storage 管理面 → **storage**；message / ai 整栈 → 对应 center；scheduler 整栈 → **quartz011 starter**。

### ③ java17-security-autoconfigure011 — 安全装配

```
infrastructure.iam.auth     # JwtAuthTokenService / PasswordEncoderAdapter / RuntimeStatusAdapter
                            #   + 无 access 时的放行 AuthorizationPort / 空 UserRoleCodes（真实 AuthorizationAdapter 在 access）
infrastructure.iam.user     # UserAuditRecorder（用户审计落地）
infrastructure.config       # KrtSecurityConfig011（krt.status / krt.jwt / krt.web.trusted-proxies + 生产启动卫兵）
web.global                  # GlobalAuthFilter（JWT 门卫；白名单经 URI 归一化匹配，含 /actuator/health）
web.global.audit            # AuditLogAspect（IUD 审计切面）
```

### ④ 中心 / 技术 starter（纵切面完整；按需引入）

```
java17-data-mybatis011-starter   # infrastructure.datasource.kernel（Druid 池 / 动态路由 / 方言 SPI 4+4 / 探针）
                                 #   + infrastructure.config.DataMybatisAutoConfiguration011（框架 Mapper 自动装配）
center-datasource011-starter     # 数据源中心：management / Sql HTTP / Sync（domain+application+infrastructure+web）；kernel 端口在 core
center-storage011-starter        # 完整存储中心：管理面 + Registry/Resolver + adapter/provider（object 端口契约在 core）
center-access011-starter         # 访问中心：organization / menu / role / perm + AuthorizationAdapter / 生产写白名单门闸
center-platform011-starter       # 平台中心：system011.{config,dictionary} 管理面（不引则无字典/配置 API）
java17-scheduler-quartz011-starter # 调度技术栈：CRUD + start/stop/runOnce + july_scheduler_audit（pk_mt）+ Quartz；
                                 #   权限目录播种在 center-access（ConditionalOnClass，见 infrastructure011/022）
center-message011-starter        # 完整消息中心：messagecenter.{inbound,outbound} + 内置出站 inapp/webhook
center-ai011-starter             # 完整 AI 中心：管理面 + 提示词 + provider 适配器 + 媒体落盘 + KrtAiConfig011
java17-observability011-starter  # TraceIdMdcFilter011 + HealthIndicator + Quartz 条件装配
```

### ⑤ java17-app011

```
com.klsjnh.app011               # Framework011Application：唯一 main（scanBasePackages 仅 demo/scheduler——
                                #   框架 bean 全部自注册，宿主零框架扫描；@MapperScan 仅 demo 包）
com.klsjnh.scheduler            # Demo011Scheduler——JobHandler 参考实现（30s tick 演示）
com.klsjnh.demo11               # ★ DDD 参考样板纵切面（第三方照抄入口）
infrastructure.iam.auth.seed    # AuthPermissionDemoSeed011（演示账号播种，已随 demo 移出框架）
resources                       # application.yml / application-development.yml（入库共享）/ logback-spring.xml
```

## 3. 命名规范

| 后缀 | 含义 | 所在层 |
|------|------|--------|
| `*011` | 本代框架标记（BasePo011 / Response011…） | 全部 |
| `*Po` | MyBatis-Plus 持久化对象 | infrastructure |
| `*Vo011` | 请求/响应载荷（入参出参都算，方向由类名语义表达） | web.<域>.vo / common.vo |
| `*Repository` / `*RepositoryImpl` | 仓储接口 / 实现 | domain / infrastructure |
| `*UseCase` | 应用层用例 | application |

## 4. 新增一个业务功能的动线

以新聚合 `Foo` 为例，按依赖方向从内到外（**完整可运行的参照物：`java17-app011` 的 `com.klsjnh.demo11` 纵切面**）。业务工程的四个分层模块照旧自建（`*-domain011 / *-application011 / *-infrastructure011 / *-web011`），框架代码只在 core / starter 里：

1. 业务 `*-domain011`：`domain.foo.Foo`（聚合根，EntityId + AuditInfo 组合）+ `FooRepository` 接口
2. 业务 `*-infrastructure011`：`FooPo`（继承 core 的 BasePo 家族）+ `FooMapper` + `FooRepositoryImpl`（基座对号入座；`@MapperScan` 写自己的 mapper 包）
3. 业务 `*-application011`：`FooUseCase`（@Transactional，编排聚合与仓储）
4. 业务 `*-web011`：`FooController` + 请求/响应 VO（@Schema 中文）
5. `docs/013.api-contract.md`：新端点涉及的契约确认
6. `docs/sql/`：建表 DDL（含公共列模板）
