# 013 — AI 中心 · 系统整体架构

> 状态：**已实现（2026-09-20）**。包名统一为 **`aicenter`**。
> 三篇一套：[011 设计思路](011.topic-design.md) · [013 系统整体架构](013.topic-architecture.md) · [015 怎么使用](015.topic-usage.md)。
> 存储依赖：[011 存储中心](../011.storage-center/013.topic-architecture.md)（产物落盘经 Port；元数据自记见 [016](../011.storage-center/016.topic-object-metadata-convention.md)）。

## 011.分层总览

```
web          AiInferenceController · AiImageController · AiAudioController · AiModelProviderController
              │
application  AiInferenceUseCase（对 service 开放）· AiImageUseCase · AiTtsUseCase · AiAsrUseCase
             AiModelProviderUseCase · AiProviderResolver · AiCapabilityRegistry
              │
domain       AiInferencePort / AiImagePort / AiTtsPort / AiAsrPort（能力 SPI）
             AiCapabilityPort（能力标记）· AiInvokeTarget · AiMedia · AiCapabilityCodes
             AiMediaStorePort / AiMediaLocation / AiMediaRef（产物落盘端口 · 调用必传定位）
             AiModelProvider / AiModelProviderApi
              ▲
                media.AiMediaStore011（→ StorageResolverPort / ObjectStoragePort）
                modelprovider.{entity,mapper,repository,probe}
```

依赖铁律不变：`web → application → domain ← infrastructure`；业务侧只依赖 domain 端口。

## 013.类清单（端口 / 能力 / 组件）

**domain（`com.klsjnh.domain.aicenter`）**

| 类 | 角色 |
|----|------|
| `capability.AiCapabilityPort` | **能力标记接口**：`capabilityCode()` + `supports(providerCode)`，各能力端口继承它 |
| `capability.AiCapabilityCodes` | 能力 code 常量（开放字符串）：`INFERENCE` / `IMAGE` / `TTS` / `ASR` |
| `capability.AiInvokeTarget` | 通用路由头：provider（id/code）+ key（id/code）+ `model`（**调用方直传**）+ `returnType` |
| `capability.AiMedia` | 通用产物：`returnType` + `url/bytes/base64` + `mimeType` |
| `inference.AiInferencePort` | 推理 SPI：`infer(cmd) → AiInferenceResult` + `stream(cmd) → Stream<AiInferenceChunk>` |
| `inference.AiInferenceCommand` / `AiInferenceResult` / `AiInferenceChunk` | 推理入参 / 同步结果 / 流式分片 |
| `image.AiImagePort` / `AiImageRequest` | 图片 SPI（文生图 + 图生图/文图生图，有输入图即后两者） |
| `audio.AiTtsPort` / `AiTtsRequest` | 语音合成 SPI |
| `audio.AiAsrPort` / `AiAsrRequest` | **语音识别 SPI（新增）** |
| `media.AiMediaStorePort` | **产物落盘端口**：`save(media, location) → AiMediaRef`（storageCode / bucket / objectKey / url） |
| `media.AiMediaLocation` | **调用必传定位**：`storageCode|storageId` + `bucketCode|bucketId`（优先 code；缺参 400） |
| `modelprovider.AiModelProvider` / `AiModelProviderApi` | 提供商聚合 + 密钥实体（**沿用**） |
| `modelprovider.AiModelProviderRepository` / `…ApiRepository` / `AiModelProviderQuerySpec` | 仓储（**沿用**） |
| `modelprovider.AiModelProbePort` | 连通性探测（**沿用**） |

**application（`com.klsjnh.application.aicenter`）**

| 类 | 角色 |
|----|------|
| `inference.AiInferenceUseCase` | 推理用例（同步 + **SSE 流式**）；**对 service 开放**（AI 开发直接注入，不走 HTTP） |
| `image.AiImageUseCase` / `audio.AiTtsUseCase` / `audio.AiAsrUseCase` | 图片 / 语音用例 |
| `modelprovider.AiModelProviderUseCase` | provider + 密钥 CRUD + 程序读入口 + 探测（**沿用**） |
| `AiProviderResolver` | provider/key 按 id 或 code 解析 + `requireModel`（**model 必传**） |
| `AiCapabilityRegistry` | **能力注册表**：收 `List<AiCapabilityPort>`，按 `(capability, provider)` 路由 |

**infrastructure（`com.klsjnh.infrastructure.aicenter`）**

| 类 | 角色 |
|----|------|
| `provider.openai.*` | **通用 OpenAI 兼容适配器**（inference / image / tts / asr 多数厂商兼容），厂商差异覆写 |
| `media.AiMediaStore011` | 实现 `AiMediaStorePort`：解析定位 → `StorageResolverPort.resolve(code).put(bucket, key, …)`（**不**读默认存储配置、**不**走管理 UseCase） |
| `modelprovider.*` | Po / Mapper / RepositoryImpl / `AiModelProbe011`（**沿用**） |

**web（`com.klsjnh.web.aicenter`）**

| 类 | 角色 |
|----|------|
| `controller.AiInferenceController` | `/klsjnh/aicenter/julyAiInference/v1`（同步 + SSE） |
| `controller.AiImageController` | `/klsjnh/aicenter/julyAiImage/v1` |
| `controller.AiAudioController` | `/klsjnh/aicenter/julyAiAudio/v1`（TTS + ASR） |
| `controller.AiModelProviderController` | provider / 密钥管理（**沿用**） |
| `converter.*` + `vo.*` | VO 转换 |

## 015.能力模块一：推理 `inference`

**定位**：AI 开发的统一入口——给提示词直接出内容。

- **端口**：`AiInferencePort`（`infer` 同步 / `stream` 流式）；
- **双开放**：`AiInferenceController`（HTTP）+ `AiInferenceUseCase`（内部注入）；
- **流式**：`stream(cmd)` 返回分片流，web 层以 **SSE**（`text/event-stream`，Spring MVC `SseEmitter`）输出；
- **不落盘**（纯文本），不必传存储定位。

```
AiInferenceCommand(baseUrl, apiKey, target, messages, temperature, maxTokens)
   → AiInferencePort.infer(...)  → AiInferenceResult(content, finishReason, usage)
   → AiInferencePort.stream(...) → Stream<AiInferenceChunk> → SSE
```

## 016.能力模块二：图片 `image`

- **文生图**：有 prompt、无输入图；
- **图生图 / 文图生图**：带 `imageUrl` / `imageBase64` 即切换（`prompt` 为编辑指令）；SenseNova 走 `/images/edits`，通用 OpenAI 兼容适配器首版仅 generations（文生图）；
- **产物落盘**：生成结果经 `AiMediaStorePort` 落地（**必传**实例+桶）；返回引用；**生命周期归使用方**（自记见 [016](../011.storage-center/016.topic-object-metadata-convention.md)）。
## 017.能力模块三：语音 `audio`

- **TTS**：文本 → 音频（产物落盘，**必传**实例+桶）；
- **ASR（新增）**：音频 → 文本。输入音频可由使用方提供本地路径 / storage 引用 / base64；识别产物是**文本**（不落盘）；
- 两者都是 `audio` 模块下的能力 code：`tts` / `asr`。

## 018.模型接入（provider + 密钥）

沿用 [requirement013/028](../../requirement013/028.topic-aicenter.md) 的主子表设计，**仅包名归位**：

- `july_ai_model_provider`（主：`provider_code` 不可变 / `provider_name` / `base_url` / `models` / `sort_order` / `status`）；
- `july_ai_model_provider_api`（密钥子表：`pk_mt` / `api_code` / `api_key` / …）；
- 密钥**类型层脱敏**（VO 不定义 `api_key` 字段），`update` 留空保持；
- 程序读入口 `getByCode` / `getApiList` / `getDefaultApi`（无 HTTP）；
- 探测 `testConnection`（HTTP 恒 200，结果在载荷）。

## 019.能力路由（代码承载，不进管理面）

**AI 管理面只管 provider + api key**——一个 api key 可对应多个模型，**模型/能力映射不在管理面建模**。

- **能力不落表、不声明**：provider 支持哪种能力，由**能力端口实现**的 `supports(providerCode)` 在**代码层**决定；
- **路由**：用例按能力找端口，命中即调用，**无实现 → 400**（现有行为）；
- "是否用了生图/生语音"**看调用**，不看配置——落盘必传定位挂在图片 / TTS 用例上。

```
AiCapabilityRegistry(List<AiCapabilityPort>)
   port(capabilityCode, providerCode) → 命中的能力端口
       ├─ 无实现 → 400
       └─ 命中 → 用例调用该端口的 invoke
```

> **加能力不改注册表**：注册表按 `(capability, provider)` 通用查表，新能力只需新增端口接口 + 实现 + code。

## 020.产物落盘（调用必传定位 · 生命周期归使用方）

```
AiImageUseCase / AiTtsUseCase
   → 生成 AiMedia（bytes/b64）
   → AiMediaStorePort.save(media, location) → AiMediaRef(storageCode, bucket, objectKey, url, …)
   → 返回引用给调用方
AiMediaStore011
   → 解析 storageCode|storageId + bucketCode|bucketId（优先 code；缺参 400）
   → StorageResolverPort.resolve(storageCode).put(bucketName, key, bytes, mime)
```

- **只依赖 Port**（`AiMediaStorePort` → `StorageResolverPort` / `ObjectStoragePort`）；**禁止**依赖存储中心管理 UseCase；
- **无默认**：不预绑 `krt.ai-center.media.storage-code`、不走实例默认桶、不强制 local011；
- **生命周期**：AI 只返回定位三元组 + URL；业务自记见 [016 元数据规约](../011.storage-center/016.topic-object-metadata-convention.md)。AI 与存储都**不做** TTL、不做定时清理。

## 021.表结构

| 表 | 说明 |
|----|------|
| `july_ai_model_provider` / `july_ai_model_provider_api` | provider + 密钥（沿用 [028](../../requirement013/028.topic-aicenter.md)） |
| `july_ai_domain` / `july_ai_domain_prompt` | **提示词主子表**（2026-09-21 重构；主=业务域树 `domain_code` 全局唯一，子=提示词 `prompt_code` 全局唯一；见 [016 提示词设计](016.topic-prompt-templates.md)） |

> DDL 唯一真源：[docs/sql/](../../sql/)（`july_center_ai.sql`）。

## 022.端点清单

前缀 `/klsjnh/aicenter`（Swagger 组 `aicenter`）：

| 模块 | 端点 | 方法 | 说明 |
|------|------|------|------|
| 推理 | `julyAiInference/v1/chat` | POST | 同步推理 |
| 推理 | `julyAiInference/v1/chat/stream` | POST | **SSE 流式**推理 |
| 图片 | `julyAiImage/v1/generate` | POST | 文生图 / 图生图 / 文图生图（有输入图即后两者） |
| 语音 | `julyAiAudio/v1/synthesize` | POST | TTS |
| 语音 | `julyAiAudio/v1/recognize` | POST | **ASR 语音识别（新增）** |
| 接入 | `julyAiModelProvider/v1/*` | — | provider / 密钥 CRUD + 探测 + export（沿用） |
| 提示词 | `julyAiDomain/v1/{insert,update,logicDelete,getById,getByCode,selectListByPage,selectTree}` | — | **主表**（业务域树）增改删查 |
| 提示词 | `julyAiDomain/v1/{saveWhole,getWithChildren}` | POST / GET | **主+子**：整存（一个事务，子表替换）/ 联查 |
| 提示词 | `julyAiDomain/v1/{insertDetail,updateDetail,logicDeleteDetail,getDetailById,getDetailByCode,selectDetailListByPage,getContent}` | — | **子表**（提示词）增改删查 |
| 提示词 | `julyAiDomain/v1/render` | POST | **渲染**（`${var}` 替换；公共件给 AI 开发） |

> **Service 入口**：`AiInferenceUseCase` / `AiImageUseCase` / `AiTtsUseCase` / `AiAsrUseCase` 均为 Spring bean，AI 开发可直接注入，不走 HTTP。
