# 017.topic-datasource（动态数据源）

> 阶段：**架构底册（横切视角）** —— 持久化之外的另一条数据通道：业务库连接注册与 SQL 路由。
> 参考来源：java17011 最新 DDD 实现（`/klsjnh/s202609/java17011`）+ java8 ci011 口径。
> **2026-09-15 修订**：本期起 **`july_datasource` 表驱动为运行时唯一真源**，`krt.ci011` 降级为引导用途。业务设计见 [requirement011/026](../requirement011/026.topic-datasource.md)，技术方案见 [requirement013/026](../requirement013/026.topic-datasource.md)。
> **2026-09-19 修订**：`DatabaseType011` 封闭枚举删除 → `DatabaseTypes011` 字符串常量；分页方言改**开放 SPI**（`SqlDialectPort011` + 注册表）；数据源仍是**动态多实例**（非 SPI），其"厂商差异"只体现在方言扩展点。

## 011.对象定义

| param011   | param013 | remark011 |
| ---------- | -------- | --------- |
| objectCode | krt.ci011 + july_datasource | 表驱动为主（管理面），yaml 为引导 |
| objectName | 动态数据源（业务库连接注册与 SQL 路由执行） | |

## 013.配置定义

### 013.1 krt.ci011（yaml，引导数据源）

启动期即需存在的库；**不再是管理对象**。字段口径见 `ConnectionInfo`。

| fieldCode | fieldName | fieldType | fieldLength | requiredField | defaultValue |
| --------- | --------- | --------- | ----------- | ------------- | ------------ |
| dsCode    | 数据源编码 | String    | 60          | true          | -            |
| dsName    | 数据源名称 | String    | 100         | false         | -            |
| dsType    | 数据库类型 | String    | 20          | true          | -            |
| dsUrl     | JDBC URL  | String    | 500         | true          | -            |
| username  | 用户名     | String    | 100         | true          | -            |
| password  | 密码       | String    | 300         | true          | -            |

```yaml
krt:
  ci011:
    - ds-code: business011
      ds-name: 业务库
      ds-type: mysql
      ds-url: jdbc:mysql://127.0.0.1:33306/db202608270001?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
      username: dev202608011
      password: dev202608011
```

### 013.2 july_datasource（表，运行时真源）

字段定义见 [requirement011/031 §013](../requirement011/026.topic-datasource.md)；DDL 见 [docs/sql/july_datasource.sql](../sql/july_datasource.sql)。

**分工红线：同一 `dsCode` 只允许一个家** —— 已在 `ci011` 声明的不得再进表，反之亦然。

## 015.方案（现有实现 + 表驱动改造）

```
java17-common011
  com.klsjnh.common.constant.DatabaseTypes011            内置 dbType **字符串常量（非枚举）**：
                                                        MYSQL / ORACLE / SQLSERVER / POSTGRESQL
                                                        + normalize(value)（trim/lower + postgres 别名）
                                                        + driverClass(value)（内置默认驱动，未知返回 null）
java17-domain011
  com.klsjnh.domain.datasource.kernel.ConnectionInfo                 record（dsCode/dsName/dsType/dsUrl/username/password
                                                                     + schemaName/driverClass/poolConfig）
  com.klsjnh.domain.datasource.kernel.DynamicDataSourceRegistryPort  register / unregister / isRegistered / testConnection
                                                                     + reloadAll(List) → ReloadResult
  com.klsjnh.domain.datasource.kernel.ReloadResult                   enabled/registered/reused/closed/failed
  com.klsjnh.domain.datasource.kernel.DataSourceProbePort            探测端口 + ProbeResult
  com.klsjnh.domain.datasource.kernel.SqlRoutingPort                 selectList / selectOne / execute / selectListByPage（读写俱备）
  com.klsjnh.domain.datasource.kernel.SqlDialectPort011              **SPI（开放扩展点，2026-09-19）**：
                                                                     dbType() 开放字符串 + pageSql(sql, offset, pageSize)
  com.klsjnh.domain.datasource.kernel.DatasourceSeedPort             播种端口 + SeedResult011
  com.klsjnh.domain.datasource.kernel.SeedResult011                  total/inserted/skipped/failed
  com.klsjnh.domain.datasource.management.JulyDatasource             聚合根（表驱动对象，见 026 主题）
  com.klsjnh.domain.datasource.management.JulyDatasourceRepository   仓储端口（+ JulyDatasourceQuerySpec）
java17-application011
  com.klsjnh.application.datasource.kernel.SqlExecuteCommand
  com.klsjnh.application.datasource.management.JulyDatasourceUseCase 数据源管理用例
java17-infrastructure011
  com.klsjnh.infrastructure.datasource.kernel.DataSourcePools011     Druid 池构造器（默认驱动 = DatabaseTypes011.driverClass(dbType)；
                                                                     显式 driverClass 优先；未知类型且无默认 → 400）
                                                                     → buildForProbe 补 maxActive 1 / maxWait 10s；isBuilt()
  com.klsjnh.infrastructure.datasource.kernel.DynamicDataSource011   AbstractRoutingDataSource + ThreadLocal 路由键 + ConcurrentHashMap 注册表（add 换旧关旧）
  com.klsjnh.infrastructure.datasource.kernel.DynamicDataSourceRegistryImpl  端口实现 → 表驱动 reloadAll（配置未变则保留旧池）
  com.klsjnh.infrastructure.datasource.kernel.DataSourceProbe011     临时池探测（不回灌注册表）
  com.klsjnh.infrastructure.datasource.kernel.SqlRoutingExecutor     JdbcTemplate + set(dsCode) / finally clear()
  com.klsjnh.infrastructure.datasource.kernel.SqlDialect011          分页方言**解析器（注册表，2026-09-19）**：注入 List<SqlDialectPort011>
                                                                     → Map<dbType, dialect>；未知 dbType → 400
  com.klsjnh.infrastructure.datasource.kernel.dialect.{Mysql,Postgresql,Oracle,Sqlserver}Dialect011  内置方言（@Component）
  com.klsjnh.infrastructure.datasource.management.entity/repository/runner/seed  JulyDatasourcePo / Impl / DatasourceSeedRunner / DatasourceSeed011
  com.klsjnh.infrastructure.persistence.mapper.CommonMapper / sql.QuotedLiteral
java17-infrastructure011（config）
  KrtConfig011 扩 ci011 绑定：List<ConnectionInfo>（type 仅要求非空 ≤20；是否为受支持库由方言注册表在分页时判定）
               启动卫兵不再用封闭枚举拒绝自定义 dbType（2026-09-19）
               ⏸ datasource.secretKey：本期未动，留二期加密存储时预埋
表驱动 reloadAll（2026-09-15 新增）
  触发：启动 / 管理面 reloadRegistry / 每次写库后自动
  → repository.findAllEnabled()（dr='0' and status='1'）
  → 逐条转 ConnectionInfo → registry.reloadAll(List<ConnectionInfo>)
       ├── 先构建 nextMap（null / 空 dsCode 跳过），再整体替换 configs
       ├── Pass 1：快照内且物理目标未变（ConnectionInfo.sameTarget：jdbcUrl/username/password/dbType 四值全等）
       │          → 保留旧池，不断连（reused++）
       ├── Pass 1：快照内但目标变了且已建池 → DynamicDataSource011.remove + pools.close（closed++）
       ├── Pass 2：旧声明集里快照中消失的（删 / 停用）→ 关池（closed++）；两 pass 不重叠，不会关两次
       └── 回报 ReloadResult { enabled, registered, reused, closed, failed } 并落一条日志
  池创建仍为懒加载（DataSourcePools011.get 的 computeIfAbsent），reloadAll 不预建新池
懒加载（决策点 1，已定案）
  启动零连接：配置仅声明；Druid 池在 dsCode 第一次真正被调用时才创建
  首次连接失败 → 该次调用抛错（illegalState → 500 信封）；未知 dsCode → badRequest
分页（决策点 2，已定案）
  SqlRoutingPort.selectListByPage(dsCode, sql, pageIndex, pageSize) → PageResult011
  pageSize 代码限死 [10, 500]（越界夹取，常量注释"产品决定"）
  方言：infra 内 SqlDialect011 解析器按开放 dbType 查 SqlDialectPort011 注册表生成页化 SQL（2026-09-19）
    mysql / postgresql  → 原SQL包子查询 + LIMIT n OFFSET m
    oracle  → rownum 双层嵌套（兼容 11g~21c，不用 12c 的 OFFSET/FETCH NEXT）
              SELECT * FROM ( SELECT klsjnh_inner.*, ROWNUM klsjnh_rn FROM ( 原SQL ) klsjnh_inner
                              WHERE ROWNUM <= offset+size ) WHERE klsjnh_rn >= offset+1
              内层别名不用 rn，避免与开发者 SQL 已暴露的 rn 列冲突
    sqlserver → 包子查询 + OFFSET m ROWS FETCH NEXT n ROWS ONLY（2012+ 原生支持）
    count = SELECT COUNT(*) FROM ( 原SQL ) cnt（大结果集由调用方优化）
yaml 播种（2026-09-15 新增）
  定位：krt.ci011 是「一次性种子」，july_datasource 表是运行时唯一真源
  入口：com.klsjnh.infrastructure.datasource.runner.DatasourceSeedRunner（ApplicationRunner，@Order 100）
        → com.klsjnh.infrastructure.datasource.seed.DatasourceSeed011（实现 domain 端口 DatasourceSeedPort）
  逐条按 dsCode 判重（existsIncludingDeleted，刻意绕过逻辑删除过滤）：
    表里没有该 dsCode            → INSERT（种子落地）
    表里有（含 dr='1' 墓碑行）    → 跳过，不更新不复活（管理页删掉的不会被重启复活）
    内容不一致（dsName/jdbcUrl/dbType）→ 表赢，记 WARN 列出差异字段
  失败策略：单条失败一律 log.error(带 dsCode + 堆栈) 后继续，绝不抛异常阻断启动
            —— 服务必须能起来，人进管理页手工修
  启动时序：Runner 先播种 → 再 reloadAll(表内容) → 注册表切到表驱动快照
           两步各自 try/catch，播种挂了仍尝试 reload
  Runner 只依赖 domain 端口，不依赖 application 层（infrastructure 不反向依赖 application）
```

- **双世界隔离（铁律）**：主数据源（MyBatis-Plus，承载 july_* 表）完全不感知动态数据源；动态库只能经 `SqlRoutingPort` 按 dsCode 访问。**注意例外**：`july_datasource` **表本身**在主库，由 MP 读写（它是"数据源的目录"，不是业务库）；
- **连接池参数**（沿 java17011）：initialSize 0 / minIdle 0 / maxActive 100 / maxWait 60s / testWhileIdle / 池化 PreparedStatement；
- **无 wall filter**（有意）：跨库 DDL / 多语句场景 wall 易误伤；
- **pom 增量**：druid-spring-boot-3-starter（版本已管理，infrastructure 引入）。

## 017.关系与联动

- `DatabaseTypes011`（**字符串常量，非枚举，2026-09-19 替代 `DatabaseType011` 枚举**）：内置驱动类名默认值 + `normalize`；显式 `driverClass` 优先，未知类型且无默认 → 400；
- `SqlDialectPort011`（SPI）：分页方言扩展点，开放字符串 `dbType` + 注册表 `SqlDialect011`；新增数据库 = 加一个方言 bean，不改底座（016 §6.5）；
- 与 KrtConfig011 的关系：ci011 与 datasource 配置组都是框架配置的一部分，绑定进 KrtConfig011（业务代码禁 `@Value` 的红线不变）；
- 与 `july_datasource` 表的关系：**表是运行时真源，ci011 是引导**（2026-09-15 起）；
- 与审计链路的关系：写表走 MP + `AuditMetaObjectHandler`（request attribute 方案），不用老项目的手工 `OperatorContext`。

## 019.与业务建模的关系（已迁出）

> **业务建模（`july_business_modeling`）已迁至低代码产品项目**（原 `dataservice011` 拆分：数据源归本域，建模归产品）。本域只做**数据源管理 + 路由 + 方言 + 探针**。

老项目 `kj17-f016_20260719_a011` 的 `july_sql` + `july_model` 由产品项目合并为 `july_business_modeling`；本项目**不建表、不维护 `july_metadata` 注册表**。

动态 SQL 的**参数化执行**（`PreparedStatement` 真绑定）→ 产品运行时能力；本域 `SqlRoutingPort` 裸 SQL（**强制 `SELECT`**）只服务管理面看数与探针。

## 021.边界外（本期不做）

- 业务建模相关（合并建表 / 模型注册表）→ **全部在低代码产品项目**，本域不做；
- 读写分离、负载均衡、多租户路由；
- 分布式事务（动态库上只跑业务查询，不参与主库事务）；
- 密码加密存储（密钥配置位本期预埋，加解密二期）。

## 023.决策点

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 启动注册失败策略 | ✅ **懒加载**：启动零连接，dsCode 第一次真正调用才建池连接；配置错误/连不上在调用时报错（2026-09-13） |
| 2 | SqlRoutingPort 能力边界 | ✅ **读写俱备 + 分页**：selectList / selectOne（读）、execute（写/DDL）、selectListByPage（方言分页，pageSize 代码限死 [10, 500]）；SQL 注入边界维持内部管理场景定位（2026-09-13） |
| 3 | 密码明文在 yaml | ✅ 生产用环境变量注入（015 配置文档既有口径）（2026-09-13） |
| 4 | 运行时真源 | ✅ **`july_datasource` 表驱动**；`krt.ci011` 降级为引导数据源；同一 dsCode 只允许一个家（2026-09-15） |
| 5 | reload 时既有池处理 | ✅ **配置未变则保留旧池**（避免改 remark 断所有连接）；变更/消失才关旧池（2026-09-15） |
| 6 | 测试连接口径 | ✅ 临时池 `initialSize 0` / `maxActive 1` / `maxWait 10s`，**不回灌注册表**，`finally` 关池（2026-09-15） |
