# 013 — IAM 中心 · 系统整体架构

> 稳态架构。设计见 [011](011.topic-design.md)，用法见 [015](015.topic-usage.md)。
> **动态权限 / opt-in / 授撤 / 启停蓝本 → [016](016.topic-dynamic-permission.md)**（本文只保留领域骨架，不展开）。
> **DDL 唯一真源**：[docs/sql/july_center_iam.sql](../../sql/july_center_iam.sql)（组织/菜单/角色/用户/权限目录共 9 表）。

## 011.领域模型

```text
july_organization（树）
        ▲ pk_org
july_user ──< july_user_role >── july_role ──┬──< july_role_permissions >── permission_code
             │                                │         ▲        ▲
             │                                │    来自菜单按钮   来自目录直授
             │                                │
july_user_audit                               │
                                              │
权限目录（详 [016]）                            │
  july_perm_object / july_perm_action
       │
       └── 菜单按钮 permission_code 宜 ∈ 目录

后期 P4：
  july_role_group ── 有效码 = Σ(直授 ∪ 组内)；内置角色全量旁路
```

- **权限码格式**：`模块:对象:动作`（如 `system011:julyScheduler:start`）。
- **内置角色**（`is_builtin='1'`）：删/停保护；菜单树与 `AuthorizationPort` 全量旁路。

## 013.主子聚合与仓库

```text
聚合① july_user（BaseMasterSubRepository）
  ├── july_user_role     toggle
  └── july_user_audit    append-only，不入级联
聚合② july_role（BaseMasterSubRepository）
  └── july_role_permissions  toggle（菜单通道 + 直授通道）
独立 july_menu / july_organization（BaseTreeRepository）
独立 july_perm_object / july_perm_action（BaseRepository 族）
```

## 015.包与 URL（现状）

| 能力 | 包 | URL |
|------|----|-----|
| 组织 / 用户 / 菜单 / 角色 / AuthorizationPort | `domain.iam.*` / `application.iam.*` / `infrastructure.iam.*` / `web.iam.*` | `/klsjnh/iam/...` |
| 权限目录 | `domain.iam.perm` | `/klsjnh/iam/julyPermCatalog/v1/...` |
| 配置 / 调度 / 字典 | `domain.system011.*` | `/klsjnh/system011/...` |

> 文档「IAM 中心」= 代码 `…iam` 包族（含组织 / 菜单，与用户 / 角色 / 权限目录对齐）。**路径变更（2026-09-24）**：`julyMenu` / `julyOrganization` 由 `/klsjnh/system011/...` 迁至 `/klsjnh/iam/...`。目录 API / `assignObjectActions` / 运行时 Port 细节见 [016](016.topic-dynamic-permission.md)。

## 017.端点概览

| 域 | 代表 |
|----|------|
| 用户 / 角色 / 菜单 / 组织 | 既有 CRUD + assignRoles / assignMenus / 树 |
| 权限目录 · 角色×动作 · 核对 | → [016](016.topic-dynamic-permission.md) |

## 019.实现状态（摘要）

| 主题 | 状态 |
|------|------|
| 用户 / 角色 / 菜单 / 组织 / JWT / 审计 | ✅ |
| 动态权限 + julyConfig / julyScheduler E2E | ✅ → [016](016.topic-dynamic-permission.md) |
| 角色组 | ⏳ P4 |
