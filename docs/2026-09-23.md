# 2026-09-23 — 工作日志

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 全仓文档↔代码审核 | ✅ | 025/032/027/031 主路径对齐；表层文档漂移见下 |
| 2 | **文档直修**（P0/P1） | ✅ | README 基座名、IAM overview 状态、015/013/016§11/结构/字典交叉引用/台账 029 等 |
| 3 | **代码债任务文档**（给其他 AI） | ✅ | [031/020](infrastructure011/031.persistence-center/020.topic-alive-unique-ddl-complete.md) |
| 4 | **020 alive_* DDL 落地 + 跑库** | ✅ | CREATE 全改；fix 补齐；dev016 MySQL 5.7.26 墓碑重插通过 |
| 5 | 字段级变更审计（全局 from→to） | ⏸ | 用户搁置，另案 |
| 6 | **supplier011 导入底座 DDL** | ✅ | 将 `docs/sql` 平台表导入 mysql013/`supplier011`（保留 yw_supplier*）；冒烟 `yw-supplier-app011` 已 Started |

## supplier011 底座导入（给业务仓）

- 目标库：`192.168.1.188:33306/supplier011`（用户 `supplier011`，口令见业务仓 yml，打码 ***WGr9）
- 源：本仓 `docs/sql/` README 顺序；`CREATE TABLE IF NOT EXISTS`；跳过 `logic-delete-unique-fix`（CREATE 已含 alive_*）与 `base-entity-columns` / demo
- 结果：30 表；`Started Supplier011Application in 4.082s`；无缺表
- 说明：无 SQL 默认 `klsjnh` 用户；登录需业务侧插 `july_user` 行；datasource/storage 由 yaml seed runner 自种子

## 口径

- 文档有问题：本会话直接改。
- 代码/SQL 有问题：只出执行文档，不改代码；提交走 `./script011.sh`（WSL `dev016`）。
