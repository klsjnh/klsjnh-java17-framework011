# 2026-09-26 — 工作日志（续：AUTH-2 / SecretCipher B3）

> 同日另有 [审计核对](2026-09-26-audit-reconciliation.md)；本篇记录 Token 吊销与消息渠道字段级加密落地。

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | **AUTH-2 / AT-05 Token 吊销** | ✅ | `july_user.token_version` + JWT claim `tv`；verify 比对版本并拒 `status=0`；status 变更 / 改密 / 重置密码 / logout bump |
| 2 | **禁用账号不可登录** | ✅ | 账号密码 + 免密路径均已拒 disabled（单测覆盖） |
| 3 | **update 可改 status** | ✅ | `JulyUserUpdateVo011.status`；变更抬版本 |
| 4 | **033 SecretCipher B3** | ✅ | `ChannelConfigCipher011`；出入站渠道 Repository 字段级加解密；VO 打码；更新 mask 保留旧密钥 |
| 5 | 文档全仓收敛 | ✅ | 033 / message-center / access-center / requirement011/015 / docs/README / sql/README / 审计核对 |
| 6 | 单测 | ✅ | JwtAuthTokenServiceTest · ChannelConfigCipher011Test · JulyUserUseCaseLoginTest |

## 运维注意

- **已有库**执行 [token-version-column.sql](sql/token-version-column.sql) 一次；新库 `july_core011.sql` 已含列。
- 消息渠道存量 `config` 明文：读兼容；下次 update 写回密文。

## 遗留

- JWT 默认 `expire-minutes: 480` 仍偏长；无 jti 单票吊销（AUTH-2 标「部分」）。
- SecretCipher 主密钥批刷 / 轮换运维流程仍可选。
- **续修**：接口一致性 P0/P1（渠道厂商密钥名、存储 mask 回写、download 鉴权、缺参 400、文档）见 [2026-09-26-api-consistency-fix](2026-09-26-api-consistency-fix.md)。
