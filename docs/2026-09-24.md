# 2026-09-24 — 工作日志

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 消息中心收敛讨论 | ⏸ | 用户搁置 |
| 2 | 企业底座缺口盘点 | ✅ | P0 = 密钥加密 + 权限编码/核对 |
| 3 | **018 IAM 中心** | ✅ | `018.iam-center`；动态权限详篇 016；P1–P3 E2E |
| 4 | **033 收口为密钥** | ✅ | SecretCipher only；权限互引 018/016 |
| 5 | 文档整体收敛 | ✅ | 台账 / SQL 命名 / IAM 四篇职责 / requirement 点引 |
| 6 | requirement013 合流 / 密钥编码 | ⏸ | 033 §018 待拍 |

## 已定口径

1. **opt-in PEP**：UseCase 挂 `assertHas` 才过滤；没挂不过滤（仍 JWT）。详 [016](infrastructure011/018.iam-center/016.topic-dynamic-permission.md)。
2. **PDP 授撤**：已挂入口上，有码放行 / 无码 403；`assignObjectActions` 下一请求生效。
3. **中心 SQL**：`july_center_<slug>.sql`（iam / ai / message）；旧 `july_*_center` / `july_perm` 等已废。
4. 密钥可逆加密独立于 IAM（033）。

## 编码与联调

| 项 | 状态 |
|----|------|
| `AuthorizationPort` + Adapter + 目录 / `assignObjectActions` | ✅ |
| julyConfig 矩阵 · Scheduler 授撤 E2E | ✅（`tools/auth-demo-matrix.sh` / `auth-dynamic-scheduler.sh`） |
| 单测 `AuthorizationAdapterTest` | ✅ |

## 文档入口

- IAM：[018.iam-center](infrastructure011/018.iam-center/011.topic-design.md)（读序 011 → 013 → 015 → **016**）
- 密钥：[033](infrastructure011/033.topic-platform-security.md)
- DDL：[sql/README](sql/README.md)

## 请确认（密钥侧 · 033 §018）

1. B0+B1 是否独立开做？
2. `accessKey` 是否加密？
3. 消息 config JSON 本迭代不做？
4. 主密钥键 `krt.crypto.master-key`？
