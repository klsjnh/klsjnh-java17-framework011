# 2026-09-25 — 工作日志

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 全项目审核（架构 / 安全 / 工程质量 / 文档四维） | ✅ | 报告在会话内；P0 安全清单见下"遗留" |
| 2 | dev016 环境验证（gate + package 含测试 + 启动冒烟） | ✅ | 重构前基线：gate PASS · 18 tests · Started |
| 3 | **模块体系重构：六模块单体 → BOM + 胖核心 + 瘦 starter（14 模块）** | ✅ | 585 路径 git mv（583 重命名，历史保留）；包名 / URL 契约 / 门禁规则零变化 |
| 4 | KrtConfig011 解散 | ✅ | → `KrtSecurityConfig011`（security-ac，含启动卫兵）/ `KrtDatasourceConfig011`（core）/ `KrtAiConfig011`（ai-starter，收编 media @Value） |
| 5 | 框架 Mapper 自动装配 | ✅ | `DataMybatisAutoConfiguration011`（data-starter imports 文件）；主类 @MapperScan 只剩 demo 包；020 quickstart 已同步 |
| 6 | 可观测性 starter（新建） | ✅ | actuator + Prometheus + traceId MDC 过滤器（白名单路径也有 traceId）+ 健康指示器×3（动态数据源池 / 存储默认行 / Quartz 条件化） |
| 7 | test011 测试套件 | ✅ | JUnit5/Mockito/AssertJ 聚合 + `BaseUseCaseTest011` 基类；存量 4 测试类随模块迁移 |
| 8 | GlobalAuthFilter 白名单修复（审核 P0-2） | ✅ | URI 归一化匹配（堵 `/v3/api-docs/../` 绕过）+ 放行 `/actuator/health`；traceId 复用 MDC 既有值 |
| 9 | 文档同步 | ✅ | README / 015.project-info / 020 quickstart / 013.structure / 015.config / 020.deploy / 017 红线 / 033 / 用法篇 / requirement 两源 13 篇（顶部注记 + 模块名映射） |
| 10 | **消费方零框架扫描（starter 自注册收口）** | ✅ | `CoreAutoConfiguration011` 定向扫描三层包 + `ObservabilityAutoConfiguration011`；主类不再扫 `com.klsjnh`；新增 `AuthChainPresenceCheck011` 熔断（web 应用缺 AuthTokenPort 拒绝启动 = 默认安全兜底） |

## 已定口径

1. **胖核心 + 瘦 starter**：特性代码全在 `java17-core011`，starter 只装技术适配；管理面 Controller 留核心（引 core 即有）。
2. **依赖方向单向**：starter → core → security-api；持久化基座家族留在 core（消环）。
3. **BOM 独立无 parent**（root import 子 BOM 会成 POM 环）；版本属性在 BOM 自持。
4. **装配单轨**：宿主不扫 `com.klsjnh`（core/starter 经 imports 自注册）；`AuthChainPresenceCheck011` 保证 web 应用必须带安全链，否则拒绝启动。
5. artifactId 沿用 011 编号；演示播种（AuthPermissionDemoSeed011）移入 reference-app——但**重置密码逻辑与密码日志仍在**（见遗留）。

## 验证（dev016）

| 项 | 结果 |
|----|------|
| `script011.sh gate`（node 正则 + tree-sitter AST + doc 检查） | PASS（582 文件 0 issue） |
| `mvn -o clean package`（**含测试**） | BUILD SUCCESS，18 tests 全绿 |
| 启动冒烟（fat jar） | Started 13.9s；`/actuator/health` = `{"status":"UP"}`；`/doc.html` 200；演示播种正常 |
| 新依赖拉取 | actuator + micrometer-prometheus 已入 dev016 m2（此后可离线构建） |

## 遗留（审核 P0，未夹带本次重构）

1. demo 播种：每次启动重置全部演示账号密码（含超管）+ 密码明文落日志（`AuthPermissionDemoSeed011`）——加环境门槛、去重置、去日志。
2. **功能级鉴权缺失**：任意登录用户可 resetPassword 超管 / 执行 SQL / 注册数据源（服务端无 URL/功能权限校验）。
3. 同步链路可写主库 `july_user`（SyncSink 目标表白名单缺失）；`SqlGuard011` 黑名单缺 `INTO OUTFILE` / `LOAD_FILE`。
4. `application-development.yml` 入库含明文凭据与可预测 JWT secret（已进 git 历史，需轮换）；`application-production.yml` 工作区明文留有真实生产库凭据（模板改占位符）。
5. 工程债：starter 条件装配完整化（现仅 data/observability 两处 imports）；gate 未跑测试（建议加 `mvn test`）；Druid 探测池泄漏（`DynamicDataSourceRegistryImpl:209`）；消息中心 inbound/outbound 克隆家族。

## 文档入口

- 模块体系：[013.structure](infrastructure011/013.topic-project-structure.md) · [015.project-info](015.project-info.md) · [README](../README.md)
- 消费方接入：[020.quickstart](020.business-project-quickstart.md)（BOM + starters + 仅业务 @MapperScan）
- 配置键拆分：[015.config](infrastructure011/015.topic-config.md)
