# 2026-09-25 — 工作日志

> **2026-09-26 口径勘误（勿被下文「遗留」原文误导）**：① `script011.sh gate` = 规范检查 + **`mvn -o clean install`（含单测）**，不是「只 compile / 不跑测试」；② `AuthPermissionDemoSeed011` 在 `java17-app011/src/test`，**fat jar 日常启动不播种**；③ 功能级鉴权已是 **双模式 PEP**（生产写白名单 / 开发 opt-in），见 [013/016](infrastructure011/013.access-center/016.topic-dynamic-permission.md)；④ 遗留勘误全文见 [重构报告 §八](2026-09-25-restructure-report.md)。以下正文保留当日事实记录。

## 任务

| # | 项 | 状态 | 说明 |
|---|----|------|------|
| 1 | 全项目审核（架构 / 安全 / 工程质量 / 文档四维） | ✅ | 报告在会话内；P0 安全清单见下"遗留" |
| 2 | dev016 环境验证（gate + package 含测试 + 启动冒烟） | ✅ | 重构前基线：gate PASS · 18 tests · Started |
| 3 | **模块体系重构：六模块单体 → BOM + 胖核心 + 瘦 starter（14 模块）** | ✅ | 585 路径 git mv（583 重命名，历史保留）；包名 / URL 契约 / 门禁规则零变化 |
| 4 | KrtConfig011 解散 | ✅ | → `KrtSecurityConfig011`（security-ac，含启动卫兵）/ `KrtDatasourceConfig011`（core）/ `KrtAiConfig011`（ai-starter，收编 media @Value） |
| 5 | 框架 Mapper 自动装配 | ✅ | `DataMybatisAutoConfiguration011`（data-starter imports 文件）；主类 @MapperScan 只剩 demo 包；020 quickstart 已同步 |
| 6 | 可观测性 starter（新建） | ✅ | actuator + Prometheus + traceId MDC 过滤器（白名单路径也有 traceId）+ 健康指示器×3（动态数据源池 / 存储默认行 / Quartz 条件化） |
| 7 | test011 测试套件（已撤） | done->removed | 曾聚合 JUnit5/Mockito/AssertJ + BaseUseCaseTest011；模块已删除，各模块直接依赖 spring-boot-starter-test |
| 8 | GlobalAuthFilter 白名单修复（审核 P0-2） | ✅ | URI 归一化匹配（堵 `/v3/api-docs/../` 绕过）+ 放行 `/actuator/health`；traceId 复用 MDC 既有值 |
| 9 | 文档同步 | ✅ | README / 015.project-info / 020 quickstart / 013.structure / 015.config / 020.deploy / 017 红线 / 033 / 用法篇 / requirement 两源 13 篇（顶部注记 + 模块名映射） |
| 10 | **消费方零框架扫描（starter 自注册收口）** | ✅ | `CoreAutoConfiguration011` 定向扫描三层包 + `ObservabilityAutoConfiguration011`；主类不再扫 `com.klsjnh`；新增 `AuthChainPresenceCheck011` 熔断（web 应用缺 AuthTokenPort 拒绝启动 = 默认安全兜底） |
| 11 | 存储 starter 改名 | ✅ | `java17-storage-local011-starter` / `java17-storage-minio011-starter`（011 跟 provider 代号走） |
| 12 | **scheduler / ai starter 真可选** | ✅ | 9 个硬依赖可选端口的 use case/controller 挂 `@ConditionalOnClass`；负向验证通过：摘掉两个 starter 应用正常启动，调度/AI 端点 404（路由级），aiDomain/config 等留守端点正常（业务 404/401）；正向全量验证 + 权限矩阵复跑全绿 |
| 13 | **存储 starter 合一** | ✅ | `java17-storage-local011/minio011-starter` → **`center-storage011-starter`**：provider 是运行时配置选择（default-type），与 message/ai 的"能力一个 starter"口径一致；4 个适配器类合并，MinIO SDK 随 starter 带上（SPI 逃生口保留） |
| 14 | **中心类 starter 统一命名** | ✅ | 三个中心改 `center-<名>011-starter`（无 java17 前缀）：center-ai011 / center-message011 / center-storage011；非中心类保持 `java17-<名>-starter011`；pom / BOM / reference-app / 全部文档同步 |
| 15 | **AI / 消息中心剥离干净** | ✅ | domain+application+infrastructure+web 整体迁入 `center-ai011-starter` / `center-message011-starter`（200 类）；AI 7 处条件注解拆除；doc 检查器改全模块扫描（旧文档零改动，20 条误报自愈）；负向验证：摘两 starter 应用照常启动，aicenter/messagecenter 整体 404 |
| 16 | **存储中心剥离** | ✅ | 管理面四层迁入 `center-storage011-starter`；object 端口契约留 core；`BackupUseCase` 改 ObjectProvider 软依赖（缺席 403 提示）；负向验证同上 |
| 17 | **金标准协议** | ✅ | [029](infrastructure011/029.topic-golden-verification.md)：G1–G8 阶梯（gate / 打包含测试 / 启动冒烟 / demo CRUD 7 步 / 调度真实触发 executeTimes 0→2 / 矩阵 / 调度授权 / 负向摘件），全部实测通过；坑清单固化（JAVA_HOME、schedulerCron、两种 404、矩阵时序） |
| 18 | **播种归位 + 消费方演示工程 + 文档全面梳理** | ✅ | ① 搬迁脚本漏掉的 `AuthPermissionDemoSeed011` 补搬进 reference-app（此前仍在 core，每个消费方都会被播种演示账号——比原判断更严重）；② 新建消费方演示工程 `java17-demo011-app011`（框架外兄弟目录，BOM+3 starters，E2E 全绿：登录/CRUD/调度 0→2）；③ 029 补坑（clean install / mysql ssl-mode）与 G9 消费方层；④ 全文档一致性清扫 |

## 已定口径

1. **胖核心 + 瘦 starter**：特性代码全在 `java17-core011`，starter 只装技术适配；管理面 Controller 留核心（引 core 即有）。
2. **依赖方向单向**：starter → core → security-api；持久化基座家族留在 core（消环）。
3. **BOM 独立无 parent**（root import 子 BOM 会成 POM 环）；版本属性在 BOM 自持。
4. **装配单轨**：宿主不扫 `com.klsjnh`（core/starter 经 imports 自注册）；`AuthChainPresenceCheck011` 保证 web 应用必须带安全链，否则拒绝启动。
5. artifactId 沿用 011 编号；演示播种（AuthPermissionDemoSeed011）移入 reference-app——但**重置密码逻辑与密码日志仍在**（见遗留）。

## 验证（dev016）

| 项 | 结果 |
|----|------|
| `script011.sh gate`（node 正则 + tree-sitter AST + doc 检查） | PASS（582 文件 0 issue） |
| `mvn -o clean package`（**含测试**） | BUILD SUCCESS，18 tests 全绿 |
| 启动冒烟（fat jar） | Started 13.9s；`/actuator/health` = `{"status":"UP"}`；`/doc.html` 200（**当日**若跑测试域 Seed 会播种；现行 fat jar 不自动 Seed——见文首勘误） |
| 新依赖拉取 | actuator + micrometer-prometheus 已入 dev016 m2（此后可离线构建） |

## 遗留（审核 P0 —— 原文保留；**现行勘误见文首 / 重构报告 §八**）

1. ~~demo 播种：每次启动重置…~~ → 现行：Seed 仅 test 域；测试场景仍可能重置+打日志。
2. ~~功能级鉴权缺失（全局）~~ → 现行：生产写白名单已落地；开发/测试仍 opt-in，未挂码入口在非 production 仍可能裸奔。
3. 同步链路可写主库 `july_user`（SyncSink 目标表白名单缺失）；`SqlGuard011` 黑名单缺 `INTO OUTFILE` / `LOAD_FILE`。
4. `application-development.yml` 入库含明文凭据与可预测 JWT secret（已进 git 历史，需轮换）；`application-production.yml` 工作区明文留有真实生产库凭据（模板改占位符）——fat jar 已 excludes production/local。
5. 工程债：starter 条件装配完整化；~~gate 未跑测试~~（已含 install/surefire）；Druid 探测池泄漏；消息中心 inbound/outbound 克隆家族。
6. **033 SecretCipher：未落地**。

## 文档入口

- **模块体系重构报告**：[2026-09-25-restructure-report](2026-09-25-restructure-report.md)（最终形态 / 决策记录 D1–D10 / 验证 V1–V6 / 遗留清单）
- 模块体系：[013.structure](infrastructure011/013.topic-project-structure.md) · [015.project-info](015.project-info.md) · [README](../README.md)
- 消费方接入：[020.quickstart](020.business-project-quickstart.md)（BOM + starters + 仅业务 @MapperScan + 必选/可选表）
- 配置键拆分：[015.config](infrastructure011/015.topic-config.md)
