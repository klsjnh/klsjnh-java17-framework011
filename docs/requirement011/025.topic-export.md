# 025.topic-export（数据导出 · 平台功能）

> 阶段：**requirement011（业务设计与业务要求）**。决策定案（2026-09-13）；**2026-09-22 加厚**：xlsx + 主子多 sheet；与 [032.topic-import](032.topic-import.md) 对称。

## 011.对象定义（ObjectInfo011）

| param011   | param013                        | remark011 |
| ---------- | ------------------------------- | --------- |
| objectCode | export                          |           |
| objectName | 数据导出（平台功能）              |           |
| objectUrl  | 各业务控制器 `/export`（**不强制**；有 Provider 即可被平台调用） |           |

## 013.请求定义

| fieldCode  | fieldName | fieldType | fieldLength | requiredField |
| ---------- | --------- | --------- | ----------- | ------------- |
| objectCode | 导出对象  | String    | 60          | true          |
| format     | 导出格式  | String    | 10          | false（默认由端点约定） |

## 015.平台机制（注册制）

```
java17-common011
  ExportFormat011                    JSON / CSV / XLSX
java17-domain011
  ExportProvider                     objectCode + columns + exportRows
                                     + sheetSpecs / exportSheetRows（默认单 sheet master）
  ExportSheet / ExportWorkbook       多 sheet 载荷
  XlsxWorkbookPort                   读写 xlsx（实现在 infrastructure）
java17-application011
  ExportProviderRegistry / ExportUseCase
  application.<域>.*ExportProvider
java17-infrastructure011
  XlsxWorkbookCodec011               Apache POI
java17-web011
  业务 Controller /export            **按需挂载**（非标配）；字典打样支持 format=xlsx 二进制下载
```

- **批大小 500**：导出按批从库拉取（循环直到取尽，**不是**总量上限/截断）；导入侧同口径作建议写批，**不因超过 500 行拒绝**；
- **敏感列红线**：Provider 不得携带 password 等；
- **必须登录**：无 operator → 401；
- EXPORT 审计；
- 新对象可导 = Provider Bean；**HTTP 端点另加**。

## 017.格式

| format | 说明 |
|--------|------|
| json | pretty-print + JSR-310；信封内 `ExportResult`（主 sheet 行，兼容旧前端） |
| csv | UTF-8 BOM + 表头（仅主 sheet） |
| xlsx | 多 sheet；表头行 = 列 `code`；主子见下表 |

**Sheet 约定**

| Sheet | 用途 |
|-------|------|
| `master` | 主表（扁平对象仅此页） |
| `children` | 子表 + 业务挂接列（如 `dictionaryCode`），禁止裸 `pk_mt` |

## 021.边界外

- 异步导出 / 任务中心 / 按过滤条件导出；
- 全对象强制挂 `/export`；
- Incremental 子资源整包进 xlsx（存储桶等）。

## 023.决策记录

| # | 决策点 | 结论 |
|---|------|------|
| 1 | 形态 | ✅ 平台注册制 Provider |
| 2 | 批大小 | ✅ 500 / 轮（非总量封顶） |
| 3 | 审计 | ✅ EXPORT |
| 4 | xlsx / 多 sheet（2026-09-22） | ✅；Controller 按需 |
| 5 | 与导入对称 | ✅ 见 032 |
