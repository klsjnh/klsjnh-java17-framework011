# 039.topic-lowcode-designer-runtime（技术方案）

> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/039](../requirement011/039.topic-lowcode-designer-runtime.md)。
> 定位：在 [038](../requirement013/038.topic-lowcode-core.md)（一主三子 CRUD）之上，补「设计 → 探测 → 发布建表 → 数据同步 → 运行时 → 开放 API」全链路；**分三期**，每期可独立演示/上线/回滚。

## 011.技术落点（分期类清单）

```
java17-domain011（包 com.klsjnh.domain.lowcode011）
  发布快照
    JulyMetadataVersion             snapshot record（objectName/version/payloadJson/physicalTable/backupTable/ddlText/publishedAt）
    JulyMetadataVersionRepository   仓储（insert / findByObjectNameAndVersion / findLatest / listByObjectName）
  DDL
    MetadataDdlGeneratorPort        generateCreate(snapshot) / generateAlter(from, to)（纯领域，无 IO）
    MetadataDdlExecutorPort         execute(dataSourceCode, ddl)（写入目标数据源；实现走 dataservice011 动态数据源）
  开放 API
    JulyMetadataOpenApi            聚合（objectName/authMode/allowedOps/apiKey...）
    JulyMetadataOpenApiRepository

java17-infrastructure011（包 com.klsjnh.infrastructure.lowcode011）
  entity/JulyMetadataVersionPo + mapper + repository            发布快照表
  entity/JulyMetadataOpenApiPo + mapper + repository            开放 API 配置表
  ddl/MySqlMetadataDdlGenerator          MySQL 方言：CREATE / ALTER ADD，白名单 + escapeIdentifier + 表名前缀
  ddl/DatasourceDdlExecutor              实现 MetadataDdlExecutorPort（复用动态数据源，仅白名单语句）
  (三期) runtime/DynamicObjectCrudMapper  动态对象 CRUD（表/列白名单 + 全绑定参数）

java17-application011（包 com.klsjnh.application.lowcode011）
  一期  JulyMetadataDesignerUseCase      listModels/load/save/probeSql/buildFromProbe/probeAndInfer/saveDraft/publishMeta/previewDdl
        JulyMetadataMetaConverter        一主三子 ↔ MetaDTO{metaData,fieldData,displayData,serviceData}（唯一映射处）
  二期  JulyMetadataPublishUseCase       publish / syncData
        JulyMetadataDataSyncUseCase      importDataFromSql / importStatus
  三期  JulyMetadataRuntimeUseCase       publishMenu / listRuntimeMenus / initialize / 动态 CRUD
        JulyMetadataOpenApiUseCase       openApiConfig / save / rotateApiKey

java17-web011（包 com.klsjnh.web.lowcode011）
  一期  JulyMetadataDesignerController   + Converter + VOs（挂 /klsjnh/lowcode011/julyMetadata/v1）
  二期  JulyMetadataPublishController    （同前缀）
  三期  JulyMetadataRuntimeController    /klsjnh/lowcode011/julyMetadata/v1 + /runtime/<objectName>
        JulyMetadataOpenApiController    + /klsjnh/open/v1/<objectName>
```

## 013.关键机制

- **MetaDTO 唯一映射**：一主三子 ↔ 老 `MetaDTO` 形状只在 `JulyMetadataMetaConverter` 一处做，供 `load/save`、`previewDdl`、`publish` 快照、运行时共用，杜绝漂移。
- **发布快照**：`publish` 把当次 MetaDTO 落 `july_metadata_version`（含 `ddl_text`），并把 `july_metadata` 的 `publish_status=PUBLISHED` / `version` 指向该版本；**DRAFT 编辑不触碰已发布快照**。
- **DDL 生成器**：`previewDdl` 与 `publish` 共用 `MetadataDdlGeneratorPort`；生成规则：首次 `CREATE TABLE IF NOT EXISTS lc_<objectName> (...)`；其后仅 `ALTER TABLE ... ADD COLUMN/INDEX/UNIQUE`；**禁止 DROP/DROP COLUMN/MODIFY/RENAME**；标识符正则白名单（`^[a-z][a-z0-9_]{0,59}$`）+ 反引号转义。
- **发布幂等/可重入**：先落版本行（PENDING）→ 执行 DDL（逐条，记录已完成）→ 成功置 PUBLISHED；重试跳过已完成语句。开关 `krt.lowcode.ddl-execute.enabled` 默认 false。
- **探测复用**：`probeSql`/`buildFromProbe` 调 `dataservice011` 的 `executeSql`（只读 + 超时 + maxRows），字段推断共用 `inferFieldData`。
- **运行时校验**：仅 PUBLISHED 对象；表/列走快照白名单；服务端按元数据校验 required/类型/长度/唯一；全部绑定参数；审计用动态 `objectCode`。

## 015.DDL 与端点

- DDL 增量：`july_metadata` 加发布态列（`publish_status`/`version`/`physical_table`/`backup_table`/`probe_sql`/`sql_code`/`data_source_code`/`data_initialized`/`last_sync_at`）；新增 `july_metadata_version`、`july_metadata_open_api`（一期一次到位）。
- 端点前缀统一 `/klsjnh/lowcode011/julyMetadata/v1`（对齐前端 `BASE=/klsjnh/lowcode011` + `actions.ts`）；运行时另有 `/runtime/<objectName>`；开放 API 另有 `/klsjnh/open/v1/<objectName>`。
- 分期端点见 [req011/039 §019](../requirement011/039.topic-lowcode-designer-runtime.md)。

## 017.改造点

| # | 改造 | 说明 |
|---|------|------|
| 1 | `july_metadata` 加发布态列 + 建两张新表 | 一期 DDL |
| 2 | `@MapperScan` 加新 mapper 包 | 装配 |
| 3 | `SpringDocConfig011` 的 `lowcode011` 组已覆盖 `/klsjnh/lowcode011/**`，无需新增 | 文档 |
| 4 | 复用 `dataservice011` 执行/探针能力 | 探测 + DDL 执行 |

## 019.任务拆解（三期）

| 期 | # | 任务 | 状态 |
|----|---|------|------|
| 一 | 1 | DDL：`july_metadata` 加列 + `july_metadata_version` + `july_metadata_open_api`（+ dev 执行） | ✅ |
| 一 | 2 | domain：快照 record + 仓储接口 + `MetadataDdlGeneratorPort` | ✅ |
| 一 | 3 | infra：快照 PO/Mapper/Repo + `MySqlMetadataDdlGenerator` | ✅ |
| 一 | 4 | application：`JulyMetadataDesignerUseCase`（MetaDTO 组装内聚，未单列 Converter） | ✅ |
| 一 | 5 | web：`JulyMetadataDesignerController`（listModels/load/save/previewDdl） | ✅ |
| 一 | 6 | 门禁 + 编译 + E2E（`bpm_user011`：save → load → listModels → previewDdl） | ✅ |
| 二 | 7 | `MetadataDdlExecutorPort` + `DatasourceDdlExecutor`（开关 `krt.lowcode.ddl-execute.enabled` + 幂等） | ✅ |
| 二 | 8 | `JulyMetadataPublishUseCase`（publish：首建 CREATE / 后仅 ADD COLUMN / 快照 / 版本 / 发布指针；syncData 待做） | ◐ |
| 二 | 9 | `JulyMetadataDataSyncUseCase`（importDataFromSql / importStatus / syncData）+ web | ⏳ |
| 二 | 10 | 门禁 + E2E（建表/ALTER/导入） | ◐ |
| 三 | 11 | 运行时动态 CRUD（白名单 + 绑定参数 + 元数据校验 + 动态审计） | ⏳ |
| 三 | 12 | `publishMenu`/`listRuntimeMenus`/`initialize` + `/runtime/<objectName>` 运行页 | ⏳ |
| 三 | 13 | 开放 API（apiKey 打码/轮换 + allowedOps + 限流 + `/klsjnh/open/v1`） | ⏳ |
| 三 | 14 | 运行时权限模型（对象级 + 操作级） | ⏳ |
| 三 | 15 | 门禁 + E2E + 文档收敛 | ⏳ |

## 020.一期 E2E（助手自测，2026-09-17）

- 用例：数据源 `bpm011`（Oracle），SQL `select id, userid, name from tuser`，对象 `bpm_user011`。
- `POST /save`（MetaDTO：10 字段 / 3 显示 / 4 服务）→ 200 + id；
- `GET /load?objectName=bpm_user011` → metaData + 10/3/4 三子；
- `GET /listModels` → 含 `bpm_user011`（publishStatus=draft）；
- `GET /previewDdl?objectName=bpm_user011` → `CREATE TABLE IF NOT EXISTS lc_bpm_user011(...)`（PK=id、类型映射正确）。
- **遗留（探针）**：033 探针带出伪列 `KLSJNH_RN`（行号）且不回别名（`id sid` 仍得 `ID`）——039 侧保存前手工剔除；建议 033 修复。

### 二期（发布）E2E（2026-09-17）

- `POST /publish`（bpm_user011）首次 → `CREATE TABLE IF NOT EXISTS lc_bpm_user011(...)`，version `0.0.1`；表建成（10 列，PK=id，类型映射正确）；
- 再次 publish → 表已存在且无缺列 → `ddl=null`、version `0.0.2`（幂等）；
- 加字段 `email` 后 publish → `ALTER TABLE lc_bpm_user011 ADD COLUMN email VARCHAR(100)`、version `0.0.3`（**后仅 ADD**）；
- `july_metadata` 指针 `PUBLISHED / 0.0.3 / lc_bpm_user011`；`july_metadata_version` 三条快照（0.0.1/0.0.2/0.0.3）。
- **开关**：`krt.lowcode.ddl-execute.enabled`（dev=true；默认 false，生产暗发）。
- **修正**：`JulyMetadataVersionPo` 继承 `BasePo011` 需 `sort_order`，两张新表已补列（DDL 文件同步）。

## 021.风险（已评审，见 req011/039 §023）

- **DDL 不可逆/半开**：幂等 + 可重入 + 开关 + 独立库/前缀；禁 MODIFY/DROP。
- **注入**：表/列/排序白名单 + 全绑定参数；`escapeIdentifier` 仅标识符。
- **权限/合规**：生产 DDL 走开关（默认关）+ DBA 审核；运行时权限模型单列。
- **探测慢查询**：只读 + 超时 + maxRows + EXPLAIN 预检。
- **version 语义**：对齐老 `version011`（`"0.0.1"` 递增字符串），前端占位递增整数需同步改。
- **E2E 污染**：独立测试库 + `lc_` 前缀隔离。
