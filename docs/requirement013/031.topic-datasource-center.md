# 031.topic-datasource-center（详细设计 · 数据源中心：读写分页 / 类型契约 / 同步 S1）

> ⚠️ 2026-09-25 模块体系重构（六模块单体 → BOM + 胖核心 + 瘦 starter）：本文旧模块名已按新归属映射（common/domain/application/web/infrastructure 层 → **java17-core011**）；技术适配现分属对应 starter：调度引擎→`java17-scheduler-quartz-starter011` · 数据源 kernel→`java17-data-mybatis-starter011` · 存储适配器→`java17-storage-local011/minio011-starter` · 消息渠道→`java17-message-starter011` · AI 适配器→`java17-ai-starter011` · JWT/过滤器/审计→`java17-security-autoconfigure011` · 安全端口→`java17-security-api011`；`java17-app011` → `java17-reference-app011`。精确对照见 [infrastructure011/013](../infrastructure011/013.topic-project-structure.md)。


> 阶段：**requirement013（详细设计 · 对接代码）** —— 数据源中心（架构类需求）的落地详细设计。
> 架构底册：[infrastructure011/017.datasource-center](../infrastructure011/017.datasource-center/013.topic-architecture.md)（011 设计 / 013 架构 / 015 用法 / 016 类型与对照契约）。
> 管理面（能力一）沿用 [026.topic-datasource](026.topic-datasource.md)。
> 状态：**能力二（参数化只读分页）+ 能力三 S1（单表同步）+ 类型契约 已实现（2026-09-20）**。

## 011.技术落点（类清单）

```
java17-core011
  constant.FieldTypeCodes011      中性类型词表（string/int/long/float/double/decimal/date/datetime/boolean/text/binary/json）
  util.SqlGuard011                只读校验（SELECT 开头 / 禁 ; / 禁 DDL-DML 关键字）
java17-core011（com.klsjnh.domain.datasource）
  kernel.ColumnMeta               列元数据 record（name/type/length/precision/scale/nullable）
  kernel.TypeDialectPort011       类型方言 SPI（columnType / quote / jdbcToNeutral / coerce）
  kernel.SqlRoutingPort           + 参数化重载（selectList/selectOne/execute/selectListByPage(..., params, ...)）
  sync.Endpoint / SyncRunResult
  sync.JulySyncRule / JulySyncRuleColumn（聚合 + 两仓储 + QuerySpec）
  sync.SyncSourcePort / SyncSinkPort / SyncEnginePort
java17-core011
  datasource.kernel.SqlQueryUseCase          只读分页查询（过 SqlGuard011）
  datasource.sync.JulySyncRuleUseCase       规则 CRUD + run · SyncColumnCommand
java17-core011
  datasource.kernel.TypeDialect011 + typedialect.{Mysql,Postgresql,Oracle,Sqlserver}TypeDialect011
  datasource.kernel.SqlRoutingExecutor      参数化实现（PreparedStatement）
  datasource.sync.SyncSource011 / SyncSink011 / SyncEngine011
  datasource.sync.{entity,mapper,repository}  JulySyncRulePo / JulySyncRuleColumnPo …
java17-core011
  datasource.controller.JulySqlController     只读分页（julySql/v1）
  datasource.controller.JulySyncController    同步规则 + run（julySync/v1）
```

## 013.能力二：参数化只读分页

- **端口**：`SqlRoutingPort.selectListByPage(dsCode, sql, params, pageIndex, pageSize)`（PreparedStatement 真绑定）；
- **校验**：`SqlGuard011.assertReadOnly(sql)`——`SELECT` 开头、禁 `;`、禁 DDL/DML 关键字；
- **clamp**：`pageSize ∈ [10, 500]`；方言分页 + `COUNT(*)`；
- **端点**：`POST /klsjnh/datasource/julySql/v1/selectByPage`（`{dsCode, sql, params[], pageIndex, pageSize}`）；
- **未来**：非 SELECT 走独立端点 `…/julySql/v1/execute` + 独立权限（分接口放权）。

## 015.类型与对照契约

> 全文见 [infrastructure011/017.datasource-center/016.topic-type-contract.md](../infrastructure011/017.datasource-center/016.topic-type-contract.md)。

- `FieldTypeCodes011` 中性词表（开放字符串）；`ColumnMeta` 列元数据；
- `TypeDialectPort011`（`columnType` / `quote` / `jdbcToNeutral` / `coerce`）+ 注册表 `TypeDialect011`，内置 mysql/pg/oracle/sqlserver；
- 对照**主子表**：主 `july_sync_rule` + 子 `july_sync_rule_column`（源列/源类型 → 目标列/目标类型）。

## 017.能力三：同步 S1（单表）

**表**：`july_sync_rule`（主）+ `july_sync_rule_column`（子）。DDL [docs/sql/july_center_datasource.sql](../sql/july_center_datasource.sql)。

**流程**：`SyncEngine011.run(rule)` → `SyncSource011.readPage`（复用参数化分页读）→ 列对照映射 → `SyncSink011.writePage`（本地表 upsert，`INSERT … ON DUPLICATE KEY UPDATE`，标识符白名单 + 反引号）→ 循环至读完。

**端点**（`/klsjnh/datasource/julySync/v1`）：`insert` / `selectListByPage` / `getByCode` / `logicDelete` / `run`。

**口径**：业务键 `sync_key`（目标侧列名，判重）；命中 → UPDATE 全列（源为准），未命中 → INSERT；`conflict=append` 不判重；`options` JSON 预留 S2/S3（`group`/`parent`/`fk`/`rowTransform`）。

## 019.改造点（方言 bug 修复）

| # | 问题 | 修法 |
|---|------|------|
| 1 | SQLServer `OFFSET/FETCH` 缺 `ORDER BY`；派生表带 `ORDER BY` 非法 | 改 **`ROW_NUMBER() OVER(...)`**（2005+）：顶层 `ORDER BY` 移入 `OVER()`，无则 `(SELECT NULL)`；序号回退常量 |
| 2 | `count` 包子查询遇 `ORDER BY` 非法 | `countSql` **下沉方言 SPI**；SQLServer 剥离顶层 `ORDER BY` |
| 3 | 结果泄漏 `KLSJNH_RN` | 执行器过滤保留列 `klsjnh_rn` |

## 021.任务拆解（已完成，2026-09-20）

| # | 任务 | 层 |
|---|------|-----|
| 1 | 类型契约（`FieldTypeCodes011` / `ColumnMeta` / `TypeDialectPort011` / 注册表 / 4 方言） | common/domain/infra |
| 2 | `SqlRoutingPort` 参数化 + `SqlRoutingExecutor` 实现 | domain/infra |
| 3 | `SqlGuard011` 只读校验 + `SqlQueryUseCase` + `JulySqlController` | common/app/web |
| 4 | 同步主子表 + 聚合/仓储/PO/Mapper + 引擎 + 用例 + 控制器 | 全 |
| 5 | 方言 bug 修复（SQLServer / count / rn 过滤） | infra |
| 6 | 门禁 + 编译 + E2E | 全 |

## 023.E2E（真机）

- **只读分页**：MySQL（参数化 `where db_type=?`）/ Oracle / SQLServer（旧版 + 2012）全 200；非 SELECT → 400、含 `;` → 400；
- **同步 S1**：bpm011（Oracle）`TUSER` **4579 行** → 本地 `lc_bpm_user011`，`read=written=4579`、10 页、**6.5 秒**；重复跑幂等；篡改本地 → 重跑被源覆盖（upsert 回写）。

## 025.风险 / 边界

- **写入方言**：sink 目前为 **MySQL**（`ON DUPLICATE KEY UPDATE`），目标固定**本地库**（`targetDsCode` 仅登记）；
- **S2/S3**：主子 / 一对多 / 多源 未实现（`options` 留口子）；
- **权限**：写端点（`execute`）未开放，机制待定；
- **`byte[]`**：大结果集/大正文有 OOM 风险（流式后续）。
