# 030.topic-aicenter-invoke（详细设计 · AI 能力：推理 / 图片 / 语音）

> 阶段：**requirement013（详细设计 · 对接代码）** —— AI 中心（架构类需求）的落地详细设计。
> 架构底册：[infrastructure011/015.ai-center](../infrastructure011/015.ai-center/013.topic-architecture.md)（011 设计思路 / 013 系统整体架构 / 015 怎么使用）。
> 状态：**已实现（2026-09-20 重构）** —— 包名 `ai011 → aicenter`、三大能力模块、通用 OpenAI 兼容适配器、推理 SSE 流式、ASR 语音识别、产物落盘 + 本地存储闸。

## 011.技术落点（类清单）

```
java17-domain011（com.klsjnh.domain.aicenter.*）
  capability.AiCapabilityPort / AiCapabilityCodes / AiInvokeTarget / AiMedia / AiReturnTypes
  inference.AiInferencePort（chat + stream）/ AiInferenceCommand / AiInferenceResult / AiInferenceChunk / AiChatMessage
  image.AiImagePort / AiImageRequest
  audio.AiTtsPort / AiTtsRequest · AiAsrPort / AiAsrRequest / AiAsrResult
  media.AiMediaStorePort / AiMediaRef
  modelprovider.AiModelProvider / AiModelProviderApi / 两仓储 / AiModelProviderQuerySpec / AiModelProbePort

java17-application011（com.klsjnh.application.aicenter.*）
  AiProviderResolver（id/code 解析 + requireModel 必传）· AiCapabilityRegistry（按 capability+provider 路由）
  inference.AiInferenceUseCase（同步 + 流式；对 service 开放，AI 开发直接注入）
  image.AiImageUseCase · audio.AiTtsUseCase / AiAsrUseCase
  media.AiMediaStorageGate（本地存储闸）
  modelprovider.AiModelProviderUseCase / AiModelProviderExportProvider

java17-infrastructure011（com.klsjnh.infrastructure.aicenter.*）
  provider.openai.OpenAiCompatInferenceAdapter / ImageAdapter / TtsAdapter / AsrAdapter（通用 OpenAI 兼容适配器）
  media.AiMediaStore011（实现 AiMediaStorePort → 存储中心 StorageResolverPort）
  modelprovider.{entity,mapper,repository,probe}

java17-web011（com.klsjnh.web.aicenter.*）
  controller.AiInferenceController（/chat 同步 + /chat/stream SSE）
  controller.AiImageController · controller.AiAudioController（synthesize + recognize）· controller.AiModelProviderController
  converter.AiMediaConverter / AiModelProviderConverter · vo.{aichat,aiimage,aiaudio,aimodelprovider}
```

> **厂商 = 一行 provider 配置**：StepFun / DeepSeek / longcat 等 OpenAI 兼容厂商由**通用适配器**承载，**无厂商专用类**；非 OpenAI 兼容的厂商实现对应能力端口（SPI）即插。

## 013.解析与调用链

```
provider 入参 ──(先 id 后 providerCode)──► AiModelProvider（baseUrl/models/status）
api 入参     ──(先 id 后 apiCode，缺省默认启用)──► AiModelProviderApi（apiKey/status）
model 入参   ── **必传**（调用方直传；框架不挑默认、不猜、不校验归属）
能力路由     ── AiCapabilityRegistry.port(capability, providerCode) → 命中的能力端口
产物         ── 图片/语音：本地存储闸 → 生成 → AiMediaStorePort.save → AiMediaRef
```

- **能力由端口实现承载**（`capabilityCode()` + `supports(providerCode)`），**不落表、不声明**；
- **本地存储闸**：图片/语音调用前检测本地实例，无 → WARN 一次 + 拒绝服务；推理（文本）不受约束；
- **生命周期归使用方**：AI 只产生并返回引用，存储只存取，**都不管何时删**。

## 015.端点

前缀 `/klsjnh/aicenter`（Swagger 组 `aicenter` / `AI中心011`）：

| 端点 | 方法 | 入参 | 出参 |
|------|------|------|------|
| `julyAiInference/v1/chat` | POST | `{provider, providerId?, api?, apiId?, model, messages[], temperature?, maxTokens?}` | `{providerCode, apiCode, model, content, finishReason, usage}` |
| `julyAiInference/v1/chat/stream` | POST | 同上 | **SSE**（逐片 `data:`） |
| `julyAiImage/v1/generate` | POST | `{provider…, model, prompt, size?, steps?, seed?, guidanceScale?, negativePrompt?, imageUrl?, imageBase64?}` | `{returnType, url, storageKey, mimeType}` |
| `julyAiAudio/v1/synthesize` | POST | `{provider…, model, input, voice?, instruction?, speed?, volume?, format?, sampleRate?}` | `{returnType, url, storageKey, mimeType}` |
| `julyAiAudio/v1/recognize` | POST | `{provider…, model, audioBase64?, audioUrl?, format?, language?}` | `{text, finishReason}` |

> **Service 入口**：`AiInferenceUseCase` / `AiImageUseCase` / `AiTtsUseCase` / `AiAsrUseCase` 均为 Spring bean，AI 开发可直接注入，不走 HTTP。

## 017.改造点（2026-09-20 重构）

| # | 改造 | 说明 |
|---|------|------|
| 1 | 包名 `ai011 → aicenter` | 四层统一；domain 按 `inference/image/audio/capability/media` 归位 |
| 2 | `chat → inference` | `AiChatPort→AiInferencePort`、`AiInvokeUseCase→AiInferenceUseCase` |
| 3 | 能力抽象 | `AiCapabilityPort` + `AiCapabilityCodes`；注册表按 `(capability, provider)` 路由 |
| 4 | 适配器归一 | 删 StepFun 专用类 → 通用 `OpenAiCompat*Adapter`；厂商 = provider 行 |
| 5 | 推理流式 | `AiInferencePort.stream` + `HttpUtil011.postJsonStream` + web SSE |
| 6 | ASR 新增 | `AiAsrPort` + `AiAsrUseCase` + 通用适配器 + `/recognize` |
| 7 | 产物落盘 | `AiMediaStorePort`/`AiMediaStore011` → 存储中心；图片/语音返回引用 |
| 8 | 本地闸 | `AiMediaStorageGate`（WARN 一次 + 拒绝） |
| 9 | 配置 | `krt.ai011.chat-timeout-seconds → krt.ai-center.chat-timeout-seconds`；新增 `krt.ai-center.media.*` |

> **无表结构变更**：管理面只管 provider + api key，**不加 `capabilities` 列**；DDL 仍 [docs/sql/july_center_ai.sql](../sql/july_center_ai.sql)。

## 019.任务拆解（已完成，2026-09-20）

| # | 任务 | 层 | 状态 |
|---|------|-----|------|
| 1 | 包名/模块归位（四层 58 文件） | 全 | ✅ |
| 2 | 能力抽象（`AiCapabilityPort`/`AiCapabilityCodes`/注册表） | domain/application | ✅ |
| 3 | 推理流式（domain 端口 + `postJsonStream` + SSE） | domain/common/infra/web | ✅ |
| 4 | ASR（端口/用例/适配器/端点） | 全 | ✅ |
| 5 | 产物落盘 + 本地闸 | domain/application/infra/web | ✅ |
| 6 | 适配器归一（删 StepFun，通用 OpenAI 兼容） | infra | ✅ |
| 7 | 门禁 + 离线编译 | 全 | ✅ |

## 021.E2E 计划（待真机验证）

1. 推理同步：`/chat` 返回内容；`model` 不传 → 400；
2. 推理流式：`/chat/stream` SSE 逐片返回、结束帧正常；
3. 图片：文生图 / 图生图 → 落盘 → 返回 `storageKey` + `url`；
4. 语音：TTS 落盘；ASR（`/recognize`）返回文本（**ASR 的 `/audio/transcriptions` 是否与厂商一致，需实测**）；
5. 本地闸：无 local011 实例时图片/语音 → WARN 一次 + 拒绝；补上后自动恢复；
6. 厂商路由：同一 provider 行换 `model` 即可切换；非 OpenAI 兼容厂商走 SPI。

## 023.风险

- **ASR 兼容性**：通用适配器假设 OpenAI 兼容 `/audio/transcriptions`，厂商差异需适配；
- **流式资源**：SSE 由 `CompletableFuture` 异步消费，未接专用线程池/超时治理；
- **`byte[]` 落盘**：大文件 OOM 风险（大文件流式后续）；
- **密钥安全**：只在服务端用，不落日志、不出参；加密二期；
- **生命周期**：使用方若忘记删除，临时文件会堆积（平台不管，属约定风险）。

## 025.推理路由头：主表 code/id + 子表 code/id + model（2026-09-20 修复）

**bug**：推理入参原只有 `provider` / `api` 单字段（"id 或 code"混在一个字段），无法分别传主表 code/id 与子表 code/id；图片/语音早已用 `AiInvokeTarget` 四字段，推理未对齐。

**修复**：

| 层 | 改动 |
|----|------|
| `AiChatRequestVo011` | 加 `providerId`、`apiId`（`provider`=providerCode、`api`=apiCode） |
| `AiInferenceUseCase` | 注入 `AiProviderResolver`；`chat`/`stream` 收 **`AiInvokeTarget`**，`resolveProvider(providerCode, providerId)` + `resolveApi(provider, keyCode, keyId)`（**先 id 后 code**） |
| `AiInferenceController` | 组装 `AiInvokeTarget(provider, providerId, api, apiId, model, null)` |

**统一口径**：推理 / 图片 / 语音 / ASR **同一套路由头** = 模型主表 code/id + 子表 code/id + `model`（**model 必传**）。

**E2E**：code 路径（`stepfun`）/ id 路径（`providerId`+`apiId`）均真实调用成功；无密钥 400、不存在 404（分别报 id / code）。

## 026.提示词管理（业务域树 + 提示词 · 2026-09-21 重构）

**表**：`july_ai_domain`（主，业务域树，`domain_code` 全局唯一）+ `july_ai_domain_prompt`（子，提示词，`prompt_code` 全局唯一）。DDL [docs/sql/july_center_ai.sql](../sql/july_center_ai.sql)。

**类**：

```
domain.aicenter.prompt
  JulyAiDomain / JulyAiDomainPrompt（聚合 + 两仓储 + QuerySpec）
  PromptRenderPort            render(promptCode, params) → 文本
application.aicenter.prompt
  JulyAiDomainUseCase（主子一个用例：主表 / 主+子 saveWhole / 子表 / render）
infrastructure.aicenter.prompt
  Po / Mapper / RepositoryImpl（主子）· PromptRender011（inline / 对象存储）
web.aicenter.controller
  AiDomainController + vo.aidomain.* + vo.aidomainprompt.*（一个模块）
```

**端点（一个模块 `/klsjnh/aicenter/julyAiDomain/v1`，三种保存粒度）**：

- **主表**：`insert` / `update` / `logicDelete` / `getById` / `getByCode` / `selectListByPage` / `selectTree`
- **主+子**：`saveWhole`（一个事务整存，子表替换）/ `getWithChildren`
- **子表**：`insertDetail` / `updateDetail` / `logicDeleteDetail` / `getDetailById` / `getDetailByCode` / `selectDetailListByPage` / `getContent`
- **渲染**：`render`

**内容口径**：

- `contentMode`：`inline`（`content` `TEXT`，≤64KB ≈2.1 万汉字）或 `storage`（超 64KB 落对象存储，选实例 + 约定桶 `ai-prompt`，复用存储中心）；
- 提示词归属一个**业务域**（`pk_mt`）；`domain_code` / `prompt_code` 均全局唯一；
- `render` 为**公共件**（AI 开发直接注入 `PromptRenderPort`），**只按提示词编码解析**。

**E2E**：建业务域 + `summary-report` → `render(system011)` / `render(lowcode011)` 的 `${text}` 正确替换；域树 / 分页 OK。
