# 023.topic-july-config（技术方案）

> ⚠️ 2026-09-25 模块体系重构（六模块单体 → BOM + 胖核心 + 瘦 starter）：本文旧模块名已按新归属映射（common/domain/application/web/infrastructure 层 → **java17-core011**）；技术适配现分属对应 starter：调度引擎→`java17-scheduler-quartz-starter011` · 数据源 kernel→`java17-data-mybatis-starter011` · 存储适配器→`center-storage011-starter` · 消息渠道→`center-message011-starter` · AI 适配器→`center-ai011-starter` · JWT/过滤器/审计→`java17-security-autoconfigure011` · 安全端口→`java17-security-api011`；`java17-app011` → `java17-reference-app011`。精确对照见 [infrastructure011/013](../infrastructure011/013.topic-project-structure.md)。


> 阶段：**requirement013（技术方案 · 双源合流唯一落点）** —— 合流 [requirement011/023](../requirement011/023.topic-july-config.md)（业务规则，三项决策已定案）与 infrastructure011（分层与基座）。
> 本主题体量小且决策全部定案，011 文档已含完整方案；本文登记技术落点与类清单。

## 011.技术落点

```
java17-core011
  com.klsjnh.domain.system011.config.JulyConfig               聚合（code 不可变 / data 全 String / Status011）
  com.klsjnh.domain.system011.config.JulyConfigRepository     仓储接口（findEnabledByCode = 程序读取入口）
java17-core011
  com.klsjnh.infrastructure.system011.config.entity.JulyConfigPo     extends BasePo，@TableName("july_config")
  com.klsjnh.infrastructure.system011.config.mapper.JulyConfigMapper extends BaseMapper
  com.klsjnh.infrastructure.system011.config.repository.JulyConfigRepositoryImpl  extends BaseRepository（business column = code）
java17-core011
  com.klsjnh.application.system011.config.JulyConfigUseCase   CRUD + getByCode（程序读取；每次查库无缓存）
java17-core011
  com.klsjnh.web.system011.controller.JulyConfigController    5 管理端点（刻意无 getByCode HTTP 端点）
  com.klsjnh.web.system011.converter.JulyConfigConverter
  com.klsjnh.web.system011.vo.julyconfig.*                    Vo011 / UpdateVo011 / QueryVo011（Upsert 内联于 Controller）
```

## 013.关键决策（引用）

- 每次查库不缓存；值全 String（300）；**`status` 可由 CRUD 设置（insert / update）与过滤（selectListByPage）**，停用对程序不可见（`getByCode` 返回 null）；`remark`（备注，≤300，可选）随 insert / update 落库；code 不可变——见 011 文档 §017 / §023。
- 与 krt.* 的边界红线见 [015.topic-config](../infrastructure011/015.topic-config.md)。

## 015.E2E 记录（2026-09-13 全过）

insert → 重复 code 400 → update → getById → 分页 keyword → logicDelete 后 getById 404。
