server:
  port: 11610

spring:
  application:
    name: java17-app011
  profiles:
    active: development
    #active: local
    #active: production
    group:
      # local 叠加在 development 之上：application-local.yml 只写本机差异，其余继承 development
      local: development
