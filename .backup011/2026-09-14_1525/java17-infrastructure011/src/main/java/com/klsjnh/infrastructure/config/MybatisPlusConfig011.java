package com.klsjnh.infrastructure.config;

/*                MybatisPlusConfig011 class
 *
 *      @author     xiangrkrs@163.com
 *      @version    ver 0.0.1
 *      @createdate 2026.09.13
 *      @modifydate
 *
 *===========================================
 *          modify history
 *
 *      2026.09.13  mybatis plus config 011 class
 *
 */

import com.baomidou.mybatisplus.annotation.DbType;
import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;
import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * MyBatis-Plus plugin configuration: WITHOUT the pagination interceptor,
 * selectPage silently returns the FULL table (no LIMIT in SQL) — a real bug
 * caught by the 100-user test data round.
 */

@Configuration
public class MybatisPlusConfig011 {

    /**
     * Pagination interceptor (MySQL dialect).
     *
     * @return mybatis-plus interceptor
     */
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));

        return interceptor;
    }
}
